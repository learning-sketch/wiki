# Wiki Log Archive — log-004

> Entries 11-16 from main `log.md` rotated here on 2026-04-19 by §11 110 KB threshold.
> All wiki internal links rewritten relative from `log-archive/`. Absolute `d:\design\...` paths unchanged.
> 6 entries / ~32 KB / period 2026-04-18 (oldest segment).
> **Indexed in main [log.md Archive Index table](../log.md)**.

---

## [2026-04-18] ingest | mindie modules/connector | module-level page (与 topics/connector.md 互补)

**触发**：用户 `ingest mindie topics P1` 三项之一（`modules/connector` 项；前两项 structured-output 与 prefix-cache 已各自有日志条目）。

**新建页**：[mindie/modules/connector.md](../mindie/modules/connector.md)（约 332 行 / ~48 KB），`status=verified`、`confidence=high`、`type=module`。

**与现有 [topics/connector.md](../mindie/topics/connector.md) 的分工**：
- topics/ 页：请求生命周期、PD KV 拉取链、Layerwise 状态机、dp_rank_id 公式（"如何流转"）
- modules/ 页：22 个文件清单 + 类/函数索引 + import/export 边界 + 跨子系统 grep 命中数（"有什么、谁用了"）

**关键发现**

1. **connector 是 backend 进程，不是同进程模块**——`setup.py:132` 注册为 `mindie_llm_backend = mindie_llm.connector.main:main` console script，由 C++ `Executor::BuildConnectorCommand` ([executor.cpp:794-809](../../MindIE-LLM/src/executor/executor.cpp)) fork 拉起；wiki 中容易误读为"text_generator 同进程子模块"，实际**独立进程**。
2. **入向 import 仅 2 处** + 1 个 entry point —— **模块边界极小**，是清晰的可重构单元。
3. **`SharedMemoryChannel` 4 + 1 channel** 名 / 大小（32MB / 0.5MB / 0.5MB）严格与 C++ `src/include/utils/shared_memory.h` 同名常量数值对齐 —— 两侧任一改动需同步。
4. **`ObjectPool` 抽象类无具体子类**（grep 0 命中）—— 预留扩展点，已加 VERIFY。
5. **`communication_type=http`** 的 CONTRADICTION：校验通过但 `SharedMemCommunication` 未实现 HTTP 分支。

**§5 step 3 5 类 hidden cross-reference grep 累积**：完整 5 类 N/A 强论断详见原 entry（已转 [log-003.md](log-003.md) 之外的更早归档）。

**净变化**：mindie/modules/ DONE 数 2 → **3**；mindie 总页数 16 → **17**。

---

## [2026-04-18] lint | wiki | post-batch acceptance（lint + ingest P1 + compare spec + verify moe）

**触发**：用户 `lint`（与本轮 `ingest mindie topics P1` / `compare speculative-decoding across all` / `verify mindie/topics/moe.md` 同一 batch）。

**本轮 batch 净增**

| 类别 | 新增 / 更新 | 文件 |
|---|---|---|
| ingest topic | 2 新页 | [mindie/topics/structured-output.md](../mindie/topics/structured-output.md) / [mindie/topics/prefix-cache.md](../mindie/topics/prefix-cache.md) |
| ingest module | 1 新页 | [mindie/modules/connector.md](../mindie/modules/connector.md) |
| compare cross | 1 新页 | [comparison/topics/speculative-decoding.md](../comparison/topics/speculative-decoding.md) |
| verify | 1 页 markers triaged + cross-check | [mindie/topics/moe.md](../mindie/topics/moe.md) |

**Lint 检查 10 项全部 PASS**，重点：
- 4 新页 frontmatter 全 `status=verified` + `confidence=high` + `verified_against=2026-04-18` + `sources` 全为绝对源码路径
- mindie/index.md 4 行 TODO → DONE 同步完成
- comparison/dimensions.md §dim 同步（structured / prefix-cache / spec / moe 四维度）
- 抽查 8 处源码 anchor 全部命中
- mermaid 语法 PASS（4 新页含 mermaid 块）

**§5 step 3 hidden cross-reference grep 累积**：~30+ N/A 强论断（mindie 端 ~15 + vllm 端 ~15+）；含本规则的页面 9 个。

**当前 wiki 状态**：MindIE 20 / vLLM 8 / SGLang 7 / comparison 10 + dimensions = **53 .md 文件**。

---

## [2026-04-18] ingest | vllm topics P1 | spec-decode-eagle

**触发**：用户 `ingest vllm topics P1` —— `spec-decode-eagle` 项（vLLM 端深化 [comparison/topics/speculative-decoding.md](../comparison/topics/speculative-decoding.md) 的 vLLM 子表）。

**新建页**：[vllm/topics/spec-decode-eagle.md](../vllm/topics/spec-decode-eagle.md)（约 578 行 / ~65 KB）。

**核心覆盖（10 节）**：12 .py 文件分类 / `SpeculativeMethod` Literal 完整 13 顶层 + 14 MTP 子枚举 / `SpecDecodeBaseProposer` 类层次（含 EAGLE / DFlash / DraftModel 3 个 base 子类 + 5 个独立 proposer）/ 双 EAGLE GPU 路径并存 / 三层占位机制 / 3 模式 `RejectionSampler` 双套并存 / NgramProposer CPU vs GPU 双实现 / DFlash + ExtractHiddenStates / 兼容性矩阵 / GPUModelRunner if/elif 链。

**关键 synthesis**：
1. **vLLM spec_decode 是"两套并存"**：老 `EagleProposer` + 老 `RejectionSampler` ↔ 新 `EagleSpeculator` + 新 `RejectionSampler`。
2. **占位机制是三层洋葱**（与 MindIE 单层 C++ placeholder + SGLang dataclass 持张量截然不同）。
3. **`mlp_speculator` 是 vLLM v1 spec 的"幽灵字面值"**：Literal 包含、文档存在、auto-detect 存在，但 proposer 选择链没分支。
4. **`extract_hidden_states` 是"伪 speculation"** —— 三家中独此一份的 spec 框架"借壳"案例。
5. **Triton kernel 数量**：vLLM spec 子系统是 v1 内 Triton kernel 密度最高的子系统之一。

**Markers**：3 CONTRADICTION + 5 VERIFY + 2 跨子系统 N/A 强论断。

**净变化**：vllm topics DONE 数 2 → **3**；总 .md 53 → **54**。

---

## [2026-04-18] ingest | vllm topics P1 | prefix-cache

**触发**：用户 `ingest vllm prefix-cache` —— 配合已建好的 [mindie/topics/prefix-cache.md](../mindie/topics/prefix-cache.md) 与 [vllm/entities/KVCacheManager.md](../vllm/entities/KVCacheManager.md)，把 vLLM 端 prefix cache 主题独立成页。

**新建页**：[vllm/topics/prefix-cache.md](../vllm/topics/prefix-cache.md)（50 KB），覆盖 hash 算法链 / `BlockHashToBlockMap` / 3 种 Coordinator 拓扑 / 6 种 `find_longest_cache_hit` 多态 / Scheduler+EngineCore 集成 / 兼容矩阵（spec decode、chunked prefill、hybrid、MLA、SW、Mamba、cross attn、LoRA、MM、prompt embeds、cache_salt、DCP/PCP）/ KV connector 协作 / KV cache events / 5 类全仓库 grep / 跨项目对照。

**关键 synthesis**：
1. **vLLM prefix cache 是 `KVCacheManager` 内置 feature**——不是独立子系统。`enable_prefix_caching=True` 默认。
2. **数据结构是 hash table** —— 与 MindIE `unordered_map<HashValue,BlockId>` 同派；与 SGLang `RadixCache`（trie）异派。**3 家两派分立**。
3. **3 种 Coordinator 拓扑**：NoPrefixCache / Unitary / Hybrid（+ EAGLE 多丢一块）。
4. **`BlockHashToBlockMap` 是 KV offload 的共享数据结构**：simple_kv_offload 直接 mirror 到 CPU——**vLLM 单语言代价换简化**设计选择的最强证据。

**Markers**：2 CONTRADICTION + 4 VERIFY + 2 跨子系统 N/A 强论断。

**净变化**：vllm topics DONE 数 2 → **3**；总 .md 53 → **54**。

---

## [2026-04-18] ingest | vllm topics P1 | kv-connector

**触发**：用户 `ingest vllm topics P1 kv-connector` —— vLLM 端 KV connector / KV offload 子系统的纵深页。

**新建页**：[vllm/topics/kv-connector.md](../vllm/topics/kv-connector.md)（~75 KB），覆盖 `KVConnectorBase_V1` 抽象类（双视角 API 表）+ **14 个 v1 backend 矩阵**（精确清点：NIXL / Mooncake / MoRIIO / LMCache×3 / HF3FS / P2pNccl / Offloading / SimpleCPUOffload / MultiConnector / FlexKV / DecodeBench / Example×2）+ scheduler/worker 双端集成 + `v1/kv_offload/` 块管理层 + `entrypoints/serve/disagg/` HTTP 前端 + 命名陷阱（do_remote_prefill/decode）+ 5 类 hidden cross-reference grep 完整结果 + 跨项目对比。

**Backend 数 verified**：之前 "13+" → 实测 **14**（[factory.py:149-228](d:\design\vllm\vllm\distributed\kv_transfer\kv_connector\factory.py) 注册表精确清点）。

**关键 synthesis**：
1. **vLLM 是三家中唯一把"PD 分离 + KV offload + 多级 cache"统一到同一个 connector 抽象**。
2. **`do_remote_prefill` / `do_remote_decode` 命名陷阱**：字段在 D 端 req 上 `do_remote_prefill=True` 意为"我（D）要去远端 P 拉 prefill"，字面意义反向。
3. **`MultiConnector` 是 "多 storage layer" 不是 "P+D 角色合并"**（resolved，源码注释闭环）。
4. **vLLM 块级失败粒度独有**：`get_block_ids_with_load_errors()` + `KVConnectorOutput.invalid_block_ids` + `kv_load_failure_policy: "recompute"|"fail"` 三件套——MindIE / SGLang 都是 req 级失败。
5. **NIXL 在 vLLM 内不只 KV connector 用**：MoE EP + all2all 也共享同一个 NIXL agent。

**Markers**：3 CONTRADICTION + 3 VERIFY + 1 跨子系统 N/A 强论断（vLLM 全树 grep `KVPoll` 0 命中 → 不用 SGLang 5 态机）。

**净变化**：vllm topics DONE 数 → **3**；comparison/topics/pd-disaggregation.md "vLLM backend 数" "13+" → **14**（精确清点）。

---

## [2026-04-18] lint | wiki | vllm topics P1 batch acceptance

**触发**：`ingest vllm topics P1` 三 subagent 并行完成后的统一 lint。

**本轮 batch 净增**：3 vLLM topics 新页（prefix-cache 50 KB / kv-connector 75 KB / spec-decode-eagle 66 KB）+ 5+ 协同更新文件。

**Lint 7 项全部 PASS**：3 新页 frontmatter / log.md append-only 时序 / vllm/index.md TODO → DONE 同步 / comparison/dimensions.md §dim 同步 / 反向 related 链接 / 死链 / mermaid 语法。

**§5 step 3 hidden cross-reference grep 累积**（vLLM 端首次实战）：3 个 subagent 都按 5 类 grep 模板执行，新增 N/A 强论断 ~15+。

**关键 synthesis 跨页发现**

1. **vLLM 14 backend > pd-disaggregation 写的"13+"**：实地 grep 已纠正。
2. **prefix-cache + kv-connector 在 vLLM 是同栈 connector hooks**：`get_num_new_matched_tokens` 在 KVCacheManager 与 connector base 之间双向调用——MindIE 是 "prefix cache plugin + connector 进程" 两栈分离的反面。
3. **spec-decode-eagle.md 与 comparison/topics/speculative-decoding.md 形成 "compare → 项目 deep dive" 的双层结构**——之前在该 compare 页中标记的 5 个 vLLM 相关 VERIFY 现在大部分可通过 spec-decode-eagle.md 的 anchor 直接 RESOLVED。

**当前 wiki 状态**：MindIE 20 / vLLM 11（+3） / SGLang 7 / comparison 10 + dimensions = **56 .md 文件**。

**Lessons learned**

1. **vLLM 是 Python-first 项目，§5 step 3 跨语言绑定一类多数为 0 命中**——但仍要写"在 X 全树 grep N 命中"强论断。
2. **数字论断要复核**：comparison 页之前写"vLLM 13+ backend"是粗略估算，深度 ingest 后实地数为 14。
3. **"compare 页驱动 entity/topic 深度 ingest" + "entity/topic 深度反哺 compare 页 RESOLVE" 是良性循环**。

---

## [2026-04-18] verify | vllm entities (EngineCoreClient + GPUModelRunner) | 2 + 3 markers triaged (5 RESOLVED + 0 active)

> 同 turn 内由 log rotation 004 增量补移（原位置在主 log 第 11 条 entry，移此以保持主 log < 110 KB 阈值）。

**触发**：用户 `verify vllm/entities/EngineCoreClient.md verify vllm/entities/GPUModelRunner.md`，承接刚完成的 vllm entities P0+P1 ingest 6 个新 entity 页中信息密度最高的 2 个。

**执行方式**：2 个 explore subagent **完全并行**，按 [AGENTS.md §6 verify](../AGENTS.md) + §7 lint + §8 anchor-driven cross-check 三套检查清单同时跑。

**Markers triaged（全部 5 个 RESOLVED）**

| 文件 | # | 类型 | 主题 | 结论 |
|---|---|---|---|---|
| EngineCoreClient.md | V1 | VERIFY | `InprocClient` 未实现基类 async API | RESOLVED：基类**仅** `shutdown` 是 `@abstractmethod`（[core_client.py:132-133](d:\design\vllm\vllm\v1\engine\core_client.py)）；其余 async API 在基类内 `raise NotImplementedError`；`InprocClient`（L274-364）未覆盖任何协程，但因 `LLMEngine` 始终 `asyncio_mode=False`，同进程模式不会触发该路径。**关键澄清**：原 VERIFY 把"未实现清单"对应到"抽象方法清单"是错误前提——抽象方法只有 `shutdown` |
| EngineCoreClient.md | V2 | VERIFY | `SyncMPClient.add_request` 与 DP 关系 | RESOLVED **并发现原假设错误**：`is_dp` 条件 `data_parallel_size > 1`；`add_request` 时置 `engines_running`、`get_output` 遇 `wave_complete` 清零。`LLMEngine` 多进程时返回 `SyncMPClient` 不按 dp 大小切换 → **DP>1 + 同步多进程仍走 `SyncMPClient`** |
| GPUModelRunner.md | C1 | CONTRADICTION | `determine_available_memory` / `compile_or_warm_up_model` 归属 | RESOLVED：源码无歧义——两方法均在 `GPUWorker` 上（[gpu_worker.py:332-482, 549-588](d:\design\vllm\vllm\v1\worker\gpu_worker.py)）；`GPUModelRunner` 仅提供 `profile_run` / `capture_model` / `load_model` 等被 Worker 调用的能力 |
| GPUModelRunner.md | V1 | VERIFY | `new_block_ids_to_zero` 与 KVCacheManager `cache_blocks` 一致性 | RESOLVED：完整生成链 scheduler → `KVCacheManager.take_new_block_ids` → `SingleTypeKVCacheManager.allocate_new_blocks`。**关键 synthesis**：`take_new_block_ids` 与 `cache_blocks` **职责正交**——前者"物理新块 GPU 清零"，后者"prefix 侧块入池/哈希" |
| GPUModelRunner.md | V2 | VERIFY | KV connector 入口行号 + mixin 调用顺序 | RESOLVED：行号已复核成 3 段——抢占处理 [3797-3800](d:\design\vllm\vllm\v1\worker\gpu_model_runner.py) / 空 batch 早退 [3831-3834] / `set_forward_context` 包 `maybe_get_kv_connector_output` + `_model_forward` [4008-4025] |

**Anchor 抽查命中率**：EngineCoreClient.md 8/8；GPUModelRunner.md 10/10（4 处 ±1 行装饰器偏差，修正 1 处 GPUWorker.md sources）。

**Lessons learned**

1. **"先深度 ingest 一批再 verify 同批"= 高 ROI 模式**：本轮 5 markers 全部 1 turn RESOLVED。
2. **"原 VERIFY 假设有误"要显式纠正**：EngineCoreClient.md V1/V2 都是 ingest subagent 留下的前提性错误。
3. **大文件 entity 的 anchor ±1 行偏差是常态**：装饰器行 vs def 行。
4. **跨家 anchor-driven cross-check 的 N/A 是高价值产出**。
5. **anchor 行号一致性可在 verify pass 同时做回链页同步修正**。

---

## [2026-04-18] ingest | vllm entities P0+P1 | LLMEngine + AsyncLLM + EngineCoreClient + GPUWorker + GPUModelRunner + OutputProcessor（6 subagent 两批并行）

> 同 turn 内由 log rotation 004 增量补移（原位置在主 log 第 12 条 entry，移此以保持主 log < 110 KB 阈值）。

**触发**：用户 `ingest vllm entities P0+P1`，承接历史 mindie entities P0/P1 的"顶层 orchestration / runtime 层"两批分组模式。

**P0/P1 分组（沿用 mindie 端 P0=顶层入口、P1=runtime 层 模式）**

| 批 | 实体 | 源文件 | 大小 |
|---|---|---|---|
| **P0** | `LLMEngine` (v1) | [v1/engine/llm_engine.py](d:\design\vllm\vllm\v1\engine\llm_engine.py) | 17 KB / 425 行 |
| **P0** | `AsyncLLM` | [v1/engine/async_llm.py](d:\design\vllm\vllm\v1\engine\async_llm.py) | 43 KB / ~1067 行 |
| **P0** | `EngineCoreClient` | [v1/engine/core_client.py](d:\design\vllm\vllm\v1\engine\core_client.py) | 70 KB / ~1696 行 |
| **P1** | `Worker` (GPU worker) | [v1/worker/gpu_worker.py](d:\design\vllm\vllm\v1\worker\gpu_worker.py) | 46 KB |
| **P1** | `GPUModelRunner` | [v1/worker/gpu_model_runner.py](d:\design\vllm\vllm\v1\worker\gpu_model_runner.py) | **314 KB / ~7k 行**（vLLM v1 单文件最大）|
| **P1** | `OutputProcessor` | [v1/engine/output_processor.py](d:\design\vllm\vllm\v1\engine\output_processor.py) | 32 KB / ~812 行 |

**新建页（6 个）**：[vllm/entities/LLMEngine.md](../vllm/entities/LLMEngine.md) / [AsyncLLM.md](../vllm/entities/AsyncLLM.md) / [EngineCoreClient.md](../vllm/entities/EngineCoreClient.md) / [GPUWorker.md](../vllm/entities/GPUWorker.md) / [GPUModelRunner.md](../vllm/entities/GPUModelRunner.md) / [OutputProcessor.md](../vllm/entities/OutputProcessor.md)

**关键 synthesis（合并 6 页）**

1. **vLLM v1 顶层 = "三层洋葱"**：`LLMEngine`/`AsyncLLM`（前端）→ `EngineCoreClient`（IPC 抽象）→ `EngineCore`/`EngineCoreProc`（后端）。
2. **真异步 vs 逻辑异步精确判定**：`AsyncLLM.output_handler` + `AsyncMPClient.process_outputs_socket` + `DPAsyncMPClient.run_engine_stats_update_task` 是真 asyncio.Task；`SyncMPClient` 用 OS 线程；`InprocClient` 完全同步。
3. **`RequestOutputCollector`** 是 vLLM v1 与 SGLang/MindIE 的关键差异点：单槽 + asyncio.Event + 流式 `aggregate=True` 合并，**不是无界 asyncio.Queue**。
4. **`Worker` 与 `GPUModelRunner` 的清晰分工**：`Worker` 负责进程级 / 设备级 / 协议级；`GPUModelRunner` 负责单步 forward / sample / spec draft 的张量级编排。`determine_available_memory` / `compile_or_warm_up_model` 在 **Worker 上**而非 ModelRunner。
5. **Worker 协议方法是字符串解析**：`WorkerProc.worker_busy_loop` 用 `getattr(self.worker, method)` —— `Worker` 类上**任何 method**都自动成为 RPC 入口。
6. **V1 vs V2 ModelRunner 双轨并存**：由 `VLLM_USE_V2_MODEL_RUNNER` env 决定。V1 单文件 ~7k 行全能；V2 极简 + `init_speculator` 工厂。
7. **`output_processor.py` 是 V1 的"全批次唯一遍历点"**——V1 vs V0 的关键架构变化。

**Markers**：5 CONTRADICTION + 9 VERIFY + 30+ §5 step 3 N/A 强论断；总 .md 文件数 63 → **69**（+6）。

详细内容见 [log-002.md](log-002.md) 同名 entry 的扩展信息（如有）；本条精简版用于 archive 节省空间。
