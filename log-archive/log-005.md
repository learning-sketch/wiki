# Wiki Log Archive 鈥?log-005

> Entries 7-10 from main log.md rotated here on 2026-04-19 (3rd rotation in <24h, P4 batch warning).
> All wiki internal links rewritten relative from log-archive/. Absolute d:\design\... paths unchanged.
> 4 entries / ~40 KB / period 2026-04-18 (mid segment).
> **Indexed in main [log.md Archive Index table](../log.md)**.

---
## [2026-04-18] batch | cross | verify vLLM P0 + ingest sglang entities P0 + compare multiproc-ipc（4 任务并行）

**触发**：用户单 turn 三命令并发：`verify vllm/entities/AsyncLLM.md / LLMEngine.md` + `compare multiproc-ipc across all` + `ingest sglang entities P0`。

**执行方式**：4 个 explore subagent **完全并行**（1 verify 合并 + 3 ingest），主 agent 同时写 `compare multiproc-ipc.md`。

### A. verify pair（AsyncLLM.md + LLMEngine.md）

**3 VERIFY + 2 CONTRADICTION 全部 triaged**

| 文件 | # | 类型 | 主题 | 结论 |
|---|---|---|---|---|
| LLMEngine.md | V1 | VERIFY | `usage_context` 实例未消费 | RESOLVED：[llm_engine.py:56](d:\design\vllm\vllm\v1\engine\llm_engine.py) 形参；`from_engine_args` 在 [llm_engine.py:163](d:\design\vllm\vllm\v1\engine\llm_engine.py) 用于 `create_engine_config`；构造体内不写实例字段——**API 兼容占位**（dead parameter on instance；config 构建期已消费） |
| LLMEngine.md | V2 | VERIFY | `use_cached_outputs` 未使用 | RESOLVED：全仓库 grep 仅 4 处（[llm_engine.py:59](d:\design\vllm\vllm\v1\engine\llm_engine.py)、[async_llm.py:80](d:\design\vllm\vllm\v1\engine\async_llm.py) + 各自 docstring）**无任何读取**；v0 兼容签名 dead parameter |
| LLMEngine.md | C1 | CONTRADICTION | arch_overview docs 漂移 | **保留有效**（非 stale）：上游 [arch_overview.md:171](d:\design\vllm\docs\design\arch_overview.md) 仍写「`LLMEngine` 在 `vllm/engine/llm_engine.py`」，与 v1 别名+主实现对照不符；wiki 加 "verify 2026-04-18 仍为旧表述" + 待上游 docs 修订 |
| AsyncLLM.md | V1 | VERIFY | `start_engine_loop` 未被 EngineCoreClient 消费 | RESOLVED：[async_llm.py:82](d:\design\vllm\vllm\v1\engine\async_llm.py) 形参 + 工厂传入 [222-223, 253-254](d:\design\vllm\vllm\v1\engine\async_llm.py)，但 `__init__` 正文 [109-202](d:\design\vllm\vllm\v1\engine\async_llm.py) **未引用**；`core_client.py` 全文 grep `start_engine_loop` **0 命中** —— **未被 EngineCoreClient 消费**（dead parameter / 未接线） |
| AsyncLLM.md | C1 | CONTRADICTION | "AsyncLLMEngine 包装 LLMEngine" docs 漂移 | **保留有效**：[arch_overview.md:173-177](d:\design\vllm\docs\design\arch_overview.md) 仍称 *asynchronous wrapper for the `LLMEngine` class*；当前 `AsyncLLMEngine` 仅为 `AsyncLLM` 别名（[async_llm_engine.py:4-7](d:\design\vllm\vllm\engine\async_llm_engine.py)），与 v1 `LLMEngine` **并列前端而非包装** |

**Anchor 抽查**：AsyncLLM **5/5 命中**；LLMEngine **5/5 命中**。**Anchor-driven cross-check**：vLLM `LLMEngine.from_vllm_config` 与 `AsyncLLM.output_handler` 在 MindIE/SGLang 全树 grep **0 命中**——三家 engine 边界不同（与 [engine-architecture.md](../comparison/topics/engine-architecture.md) 一致），**N/A 强论断**。

### B. ingest sglang entities P0（3 个新页）

| 文件 | 大小 | 关键涵盖 |
|---|---|---|
| [sglang/entities/Engine.md](../sglang/entities/Engine.md) | ~20 KB | `Engine(EngineScoreMixin, EngineBase)` + `HttpServerEngineAdapter` 独立实现非 wrapper；`__init__` 8 步装配 + `_launch_subprocesses` 完整流程 + `_launch_scheduler_processes`（DP 时 fork DataParallelController）+ 25+ API 表（generate/encode/score/LoRA/weight_update/profile）+ 可覆盖类属性（`server_args_class` / `init_tokenizer_manager_func` / `run_scheduler_process_func` 用于 RayEngine 子类） |
| [sglang/entities/TpModelWorker.md](../sglang/entities/TpModelWorker.md) | ~17 KB | **从 Scheduler.md 拆出独立页**：`BaseTpWorker` 抽象 + `TpModelWorker(BaseTpWorker)` 主实现；9 步 `__init__`；**`forward_batch_generation` 4 分支**（DLLM / PP 末段-非末段 / overlap+grammar `delay_sample_func` closure / prefill-only dummy）；**`get_memory_pool` 二元组**（`req_to_token_pool, token_to_kv_pool_allocator`，spec decode draft 与 target 共享 KV pool 的关键）；与 vLLM Worker / MindIE ModelRunner 三家对照表 |
| [sglang/entities/DataParallelController.md](../sglang/entities/DataParallelController.md) | ~19 KB | `LoadBalanceMethod` 4 策略枚举（ROUND_ROBIN / FOLLOW_BOOTSTRAP_ROOM / TOTAL_REQUESTS / TOTAL_TOKENS）+ `DPBudget` 负载追踪；`__init__` 6 步；**外部 `routed_dp_rank` 钉死路由**（绕过 LB）；`event_loop` 仅 node_rank==0 跑；`launch_dp_schedulers` vs `launch_dp_attention_schedulers` 双分支；与 vLLM `DPLBAsyncMPClient`（前端进程内）/ MindIE `is_dp_and_server_centralized`（generator 内 flag）三方对照——**仅 SGLang 用独立子进程做 DP 路由** |

### C. compare multiproc-ipc（主 agent 直接写）

**新建页**：[comparison/topics/multiproc-ipc.md](../comparison/topics/multiproc-ipc.md)（约 26 KB / 350 行 / 9 子维度）

**9 子维度**

1. 进程边界数与 IPC 拓扑（含三家 mermaid）
2. IPC 协议栈对照（vLLM 5+1 类 / SGLang 1 类 / MindIE 1 类）
3. 序列化机制（msgspec+cloudpickle / pickle / protobuf）
4. 零拷贝路径（vLLM 全链零拷贝 / MindIE 通道零拷贝 protobuf 编码有拷贝 / SGLang 全链 pickle 必拷）
5. ready 同步与 death 检测（含三家 ready handshake / death detection / failure 帧 / 优雅关闭三段式对照）
6. ZMQ socket 类型对照（DEALER / PUSH / XSUB / PAIR / ROUTER vs 仅 PUSH/PULL）
7. 综合 cheat sheet（11 行覆盖所有关键差异）
8. Anchor-driven cross-check（3 anchor × 三方反向扫，**6 处 N/A 强论断**：MessageQueue / send_pyobj / channel 名）
9. 与 PD 优化的关联（synthesis：6 条可借鉴优化思路）

**关键 synthesis（综合三家差异）**

1. **vLLM 是 IPC 形态最丰富的**：5 类机制（ZMQ + MessageQueue 共享内存 + Pipe + queue.Queue + POSIX 信号）+ TensorIPC 第 6 类。前端 ↔ EngineCore 用 ZMQ，Executor ↔ Worker 走 `MessageQueue` **零拷贝共享内存热路径**。msgspec + cloudpickle 双序列化器。
2. **SGLang 只用 ZMQ + pickle**：所有 manager 间通信都是 PUSH/PULL + `recv_pyobj` / `send_pyobj`，**全 IPC 路径无零拷贝**。被 [TokenizerManager.md CONTRADICTION](../sglang/entities/TokenizerManager.md) 标"吞吐 ceiling 受 pickle 性能影响"。
3. **MindIE 只在 generator ↔ connector 之间有 IPC**：4 channel 名（execute / shared_sync_link / transfer / recover_command）+ 严格大小常量（32MB / 0.5MB / 0.5MB），C++ 与 Python **双向数值对齐**。其它路径都是同进程线程 + Python 函数调用。
4. **三家"哪些边界跨进程"决定 IPC 复杂度**：vLLM 进程边界最多（前端/EngineCore/N×Worker）→ IPC 机制最丰富；SGLang 严格 3 进程 → 单一协议；MindIE 几乎只 1 进程 → IPC 仅为 PD connector 子进程留。
5. **`MessageQueue` 共享内存 + msgspec 是 vLLM 唯一拥有的"零拷贝 + 灵活"双优势**：MindIE 受限于 protobuf schema + C++ 跨语言；SGLang 选择最简单的 pickle 但承担吞吐代价。
6. **3 anchor × 三方反向扫 = 6 处 N/A 强论断**：三家 IPC 在协议、序列化、命名、数据流模型层面都正交，**没有可直接套用的等价物**。

### 协同更新

| 文件 | 修改 |
|---|---|
| [sglang/index.md](../sglang/index.md) | 4 行 TODO → DONE：Engine / TpModelWorker（拆出独立页）/ DataParallelController；剩余实体加 P1/P2/P3 优先级标 |
| [sglang/entities/Scheduler.md](../sglang/entities/Scheduler.md) frontmatter `related:` | 加 TpModelWorker.md / Engine.md / DataParallelController.md |
| [comparison/index.md](../comparison/index.md) | (a) 待建表新增 Multi-process IPC 行 DONE；(b) "已建对比页" 表新增 Multi-process IPC 行；(c) frontmatter `related:` 加 multiproc-ipc.md |
| [wiki/index.md](../index.md) | 跨项目对比表加 Multi-process IPC 行；快速跳转表 "主对象" / "模型执行器" 列升级到新 sglang/vllm entity 页（Engine / LLMEngine+AsyncLLM / TpModelWorker / GPUWorker+GPUModelRunner） |
| [vllm/entities/LLMEngine.md](../vllm/entities/LLMEngine.md) | 2 VERIFY → RESOLVED；CONTRADICTION 加 "verify 2026-04-18" 标注；frontmatter `verified_against` 加 verify pass；TODO 占位删 |
| [vllm/entities/AsyncLLM.md](../vllm/entities/AsyncLLM.md) | 1 VERIFY → RESOLVED；CONTRADICTION 同上加标注 |

### Markers added（本轮所有新页 + verify 累计）

| 类型 | 数量 | 文件分布 |
|---|---|---|
| `[!todo] VERIFY` | **8** | Engine（多节点 node_rank!=0 / RayEngine 覆盖）/ TpModelWorker（BaseTpWorker Liskov / model_runner_list 非 multi-layer EAGLE 路径）/ DataParallelController（TOTAL_TOKENS 闭环）/ multiproc-ipc（MessageQueue 协议 / SGLang DP 端口双重身份 / MindIE 第 5 channel） |
| `[!warning] CONTRADICTION` | **4** | Engine（HttpServerEngineAdapter API 面窄于 Engine 但 dimensions.md 暗示并列）/ multiproc-ipc（vLLM death 检测最 robust 是 synthesis、SGLang pickle ceiling 是推断）/ LLMEngine + AsyncLLM 各 1（保留并加"verify 2026-04-18 仍为旧表述"）|
| 跨子系统 N/A 强论断 | **15+** | sglang 3 entity 各 5 类 §5 step 3 grep + multiproc-ipc §8 anchor cross-check 6 处 |
| Markers RESOLVED 本轮累计 | **5** | LLMEngine V1+V2、AsyncLLM V1（**全部 RESOLVED**）|

### 净变化

| | before | after |
|---|---|---|
| sglang entities/ 页数 | 2 | **5**（+3：Engine、TpModelWorker、DataParallelController） |
| 三家 entities 总数 | MindIE 9 + vLLM 10 + SGLang 2 = 21 | MindIE 9 + vLLM 10 + SGLang **5** = **24** |
| comparison topics DONE 数 | 12/24 | **13/24**（+ multiproc-ipc） |
| `wiki/index.md` 跨项目对比表 DONE 行 | 11 | **12** |
| sglang `Engine` / `TpModelWorker` / `DataParallelController` 集中 anchor | 散在 Engine launcher 段 / Scheduler.md 合并段 / 仅 distributed.md 一句 | **集中在 3 个独立 entity 页** |
| LLMEngine / AsyncLLM markers active | 2 VERIFY + 1 CONTRADICTION + 1 VERIFY + 1 CONTRADICTION = 5 | **2 active CONTRADICTION + 3 RESOLVED**（CONTRADICTION 因上游 docs 未修而保留）|
| 总 .md 文件数 | 71 | **75**（+4：3 sglang entity + 1 multiproc-ipc compare） |

### Lessons learned

1. **"4 任务并行" 是单 turn 高 ROI 模式**：本轮 1 verify pair（合并 1 subagent）+ 3 sglang entity ingest（3 subagent）+ 1 主 agent 直接写 compare 页 = 4 路并行，全部在 1 turn 内完成 + 协同更新 + log entry。**子任务相互独立时建议优先并行**而非串行。
2. **"verify 上游 docs 漂移"要主动 read docs 确认现状**：本轮 verify pair 中两处 CONTRADICTION 都涉及 `docs/design/arch_overview.md`，subagent 主动 read 该 docs 确认上游**仍为旧表述**，故 CONTRADICTION 标"保留有效非 stale"+ "verify 日期"。这种"docs vs code 漂移"的 verify 必须 read 双方原文才能精确判断。
3. **"拆出独立页 + 反向回链" 是渐进式 wiki 演化模式**：本轮 TpModelWorker 从 [Scheduler.md](../sglang/entities/Scheduler.md) 拆出独立页，Scheduler.md frontmatter `related:` 加 TpModelWorker.md，§See also 互链。**这种渐进拆分比一次性 ingest 多个 entity 更自然**——先合并写一遍，等需要深化某个子实体时再拆出。
4. **"主 agent 直接写 compare" vs "subagent 写"的选型**：本轮 compare multiproc-ipc 主 agent 直接写（基于 4 个相关 wiki 页 + 3 个 entity 页素材），未启用 subagent；理由是相关 anchor 已就绪、主 agent 综合视角好。**"compare 页基于已有 wiki 综合"应优先主 agent 直接写**；只有需要大量新 grep 探索时才启 subagent。
5. **`HttpServerEngineAdapter` 是独立实现非 wrapper**：subagent 主动发现这点（[`http_server_engine.py:49-61`](d:\design\sglang\python\sglang\srt\entrypoints\http_server_engine.py)），而 [comparison/dimensions.md §dim-engine](../comparison/dimensions.md) 之前写 "`Engine, HttpServerEngine`" 暗示并列入口（实际 Adapter 是受限 HTTP 包装）；本页加 CONTRADICTION 标记。**dimensions.md cell 简化描述容易掩盖类间关系**，深度 ingest 一致校正。
6. **DP 控制器 3 家 3 种位置**（独立子进程 / 前端进程内 / generator 内 flag）是 [comparison/topics/distributed.md](../comparison/topics/distributed.md) 现有 §dim-distributed 的重要补充——本轮 DataParallelController.md 已铺好对照表种子。

### 当前 wiki 状态

| 项目 | overview | index | modules | entities | topics | 总数 |
|---|---|---|---|---|---|---|
| MindIE-LLM | ✅ | ✅ | 3/14 | 9/9 | 8/13 | 20 |
| vLLM | ✅ | ✅ | 2/26 | 10/17 | 5/14 | 17 |
| SGLang | ✅ | ✅ | 3/30 | **5/11**（+ Engine + TpModelWorker + DataParallelController） | 2/14 | **10** |
| comparison | — | ✅ | — | — | **13/24**（+ multiproc-ipc） | **13 + dimensions** |

**总 .md 文件数**：75（+4）。

### 下一步建议

按 ROI 排序：

1. **`compare prefix-cache across all`** —— mindie + vllm 双 prefix-cache 页 + sglang mem_cache.md 已就绪，"hash table vs trie 三家两派"的 synthesis 种子已在两个 prefix-cache 页中明示
2. **`ingest sglang entities P1`**（`SchedulePolicy` / `ScheduleBatch` / `CacheController` / `DisaggService`）—— 4 个剩余 P1 候选，与 SGLang topic 深化配套
3. **`compare kv-cache across all`** —— vLLM kv-connector + sglang mem_cache HiCache + mindie kv-cache 三家深度页全就绪，可建跨家 cross compare
4. **`verify comparison/topics/engine-architecture.md` / `executor-worker.md` / `multiproc-ipc.md`** —— 本轮 3 个 compare 页累积 8+ markers 待消化
5. **被动等待**：三仓 HEAD 都未动，无需触发 §12 增量

---

## [2026-04-18] archive | wiki | log rotation 003（第 2 次轮转，按 §11 110 KB 阈值触发）

**触发**：`compare executor-worker across all` entry append 后 log.md 达到 ~115 KB / 1099 行 —— 超 [AGENTS.md §11](../AGENTS.md) 110 KB 字节阈值（行数 1099 < 1500 阈值，但**字节阈值任一满足即触发**）。

**轮转结果**

| 文件 | 期间 | entries | 大小 | 备注 |
|---|---|---|---|---|
| **新建** [log-archive/log-003.md](./log-003.md) | 2026-04-18 中段 | 4 | ~50 KB | archive log rotation 001/002 + pin source-versions + lint post pin/rotation health check + verify comparison/topics/speculative-decoding |
| **重写** [log.md](log.md) | 当前 | 10 + 本条 = **11** | ~92 KB | header + Archive Index（含 log-003 新行）+ 10 最近 entries + 本 archive 条目 |

**转移内容**（按 §11 保留策略）

主 log.md 保留最近 10 条 entries（涵盖 ingest/verify/compare/lint 各类），最早的 4 条转入 log-003.md：

| 转移条目 | 原 line 范围 | 操作日期 | 类型 |
|---|---|---|---|
| archive | wiki | log rotation 001/002（按方案 A 触发） | 822-862 | 2026-04-18 | archive |
| pin | source-versions | initial pin + 增量工作流 schema 沉淀 | 864-911 | 2026-04-18 | pin |
| lint | wiki | post pin/rotation health check（含 §11/§12 首次集成验证） | 913-994 | 2026-04-18 | lint |
| verify | comparison/topics/speculative-decoding | 5 markers triaged | 996-1056 | 2026-04-18 | verify |

**保留在主 log 的 10 条最新 entries**

| # (newest→oldest) | 操作 | scope | 摘要 |
|---|---|---|---|
| 1 | compare | cross | executor-worker across all 3 projects（11 子维度，本轮新建）|
| 2 | compare | cross | engine-architecture across all 3 projects（13 子维度）|
| 3 | verify | vllm entities | EngineCoreClient + GPUModelRunner（5 markers RESOLVED）|
| 4 | ingest | vllm entities P0+P1 | 6 entity 页两批并行（LLMEngine/AsyncLLM/EngineCoreClient + GPUWorker/GPUModelRunner/OutputProcessor）|
| 5 | ingest | mindie modules/connector | module-level 页（与 topics/connector.md 互补）|
| 6 | lint | wiki | post-batch acceptance（lint + ingest P1 + compare spec + verify moe）|
| 7 | ingest | vllm topics P1 | spec-decode-eagle |
| 8 | ingest | vllm topics P1 | prefix-cache |
| 9 | ingest | vllm topics P1 | kv-connector |
| 10 | lint | wiki | vllm topics P1 batch acceptance |

**链接修复**

归档文件 [log-003.md](./log-003.md) 内的 wiki 路径已修复为相对从 `log-archive/` 出发（即 `../comparison/...` / `../mindie/...` / `../vllm/...` / `../sglang/...` / `../AGENTS.md` / `../source-versions.md` 等）；`d:\design\<...>` 绝对路径不变。

**净变化**

| | before | after |
|---|---|---|
| log.md 行数 | 1099 | ~145（节省 ~87%）|
| log.md 字节 | ~115 KB | ~92 KB（合规：< 110 KB 阈值）|
| log.md entries | 14 | **10 + 本条 = 11** |
| 归档文件数 | 2（log-001 + log-002）| **3**（+ log-003）|
| 归档总字节 | ~180 KB | ~230 KB（与 log.md 历史完全一致，无信息丢失）|

**与上次轮转（log-001/002）对比**

| 维度 | 上次轮转 | 本次轮转 |
|---|---|---|
| 触发原因 | 用户反馈"log.md 过大"（手动）| `compare executor-worker` entry append 后字节超阈值（自动检测）|
| 轮转前 log.md 大小 | 1971 行 / 143 KB / 44 entries | 1099 行 / 115 KB / 14 entries |
| 转移条目数 | 38（→ log-001 21 条 + log-002 17 条） | 4（→ log-003 4 条）|
| 主 log 轮转后大小 | ~600 行 / ~85 KB / 7 entries | ~145 行 / ~92 KB / 11 entries |

**Lessons learned**

1. **轮转节奏快于预期**：上次 4-18 中段轮转后，仅 ~6 小时（同日内）就再次触发。原因是 vLLM entities P0+P1 ingest（6 个 entity 页 + 详细 lessons learned）+ 双 verify pass + 双 compare（engine-architecture + executor-worker）每条 entry 都很大（约 8-12 KB）。**当前节奏下 log 轮转日均 1-2 次很正常**，不必担心。
2. **保留策略 "最近 10 条" 是合适粒度**：本轮转后主 log 仍含 11 条 entries（含本 archive 条目），覆盖最近 1-2 天工作上下文，新 agent 接手时无需读 archive 即可了解最近上下文。
3. **archive 操作 entry 模板可固化**：本 entry 沿用 [archive 001/002 entry](./log-002.md) 模板（触发 / 轮转结果表 / 转移内容表 / 保留条目表 / 链接修复说明 / 净变化表 / lessons），是良好的可复用 schema。建议 [AGENTS.md §11](../AGENTS.md) 末尾加一句"archive entry 应包含至少 6 段：触发原因 / 轮转结果 / 转移条目 / 保留条目 / 链接修复 / 净变化"。
4. **`log-003.md` 大小约 50 KB，远小于 log-001/002 的 70-110 KB**——印证小批量轮转（4 条 vs 17-21 条）是健康节奏，单 archive 文件不会过大。

**下一次轮转预期**

按当前节奏（每天 ~10 entries），预计 **1-2 天内**会再次触发 → log-004.md。建议 ingest 节奏稳定后逐步进入"每周 1-2 次轮转"的稳态。

---

## [2026-04-18] compare | cross | executor-worker across all 3 projects

**触发**：用户 `compare executor-worker across all`，承接 [engine-architecture.md §5 Engine↔Worker 边界](../comparison/topics/engine-architecture.md) 已铺好的种子，深化为独立对比页。

**新建页**：[comparison/topics/executor-worker.md](../comparison/topics/executor-worker.md)（约 540 行 / ~36 KB），`status=verified`、`confidence=high`、`type=comparison`、`project=cross`。覆盖 11 个子维度。

**11 子维度**

1. **Executor 抽象层**（vLLM 唯一显式抽象 + 4 实现 vs MindIE 无 vs SGLang 无）
2. **Worker 类层次与字段表**（vLLM `Worker(WorkerBase)` + `model_runner: GPUModelRunner` / MindIE 无独立 Worker 类 / SGLang `TpModelWorker(BaseTpWorker)` + `model_runner_list: List[ModelRunner]`）
3. **Worker 进程模型**（独立进程 vs 同进程线程 vs scheduler 同进程对象；含进程命名 / 死亡监控 / 死亡传播 / 优雅关闭对比）
4. **RPC / 调用机制**（vLLM `MessageQueue.enqueue` + `getattr(self.worker, method)` 字符串 RPC + cloudpickle callable 序列化 / MindIE 同进程函数调用 / SGLang scheduler↔worker 同进程函数 + manager 间 ZMQ pickle）
5. **forward 路径与 sampler 拆分**（**vLLM 唯一拆为两 method** `execute_model` + `sample_tokens` / MindIE 单 method / SGLang 同 method + 可选 `delay_sample_func` closure）
6. **多 worker 并行机制**（同进程多线程 HCCL / 多进程 NCCL + MessageQueue / 多 scheduler 进程 NCCL）
7. **启动序列对照**（vLLM 12 步 + worker 子进程 9 步 / MindIE Generator.__init__ 内嵌 / SGLang TpModelWorker 9 步 + Engine launcher）
8. **错误处理与传播**（vLLM FAILURE 帧 + traceback add_note + monitor_workers 三段式 SIGTERM / SGLang scheduler 子进程异常 + watchdog / MindIE 同进程异常）
9. **综合对照 cheat sheet**（13 行覆盖所有关键差异）
10. **Anchor-driven cross-check**（3 anchor × 三方反向扫，含"draft model runner 持有位置"差异：SGLang/MindIE 在 worker 层 vs vLLM 在 model runner 层）
11. **与 PD 优化的关联**（synthesis：6 条可借鉴的优化思路）

**关键 synthesis（综合三家差异）**

1. **vLLM 唯一拥有显式 `Executor` 抽象**（[abstract.py](d:\design\vllm\vllm\v1\executor\abstract.py)）+ 4 实现（UniProc / MultiProc / Ray / RayV2）覆盖 inproc / 多进程 / Ray / RayV2 四种部署形态——这是为了支撑可插拔分布式后端；MindIE/SGLang 不抽象因为各自固定走单一并行方案（HCCL 多线程 vs NCCL 多进程），没有"换 backend"的需要。
2. **抽象层级数量**：MindIE = 3 层（Generator→PluginManager→ModelRunner）/ vLLM = **6 层**（LLMEngine→EngineCore→EngineCoreClient→Executor→Worker→GPUModelRunner）/ SGLang = 4 层（Engine→Scheduler→TpModelWorker→ModelRunner）—— vLLM 抽象层级最深，对应"最灵活但学习曲线最陡"。
3. **forward+sample 拆分原因 = async scheduler 必须**（synthesis）：vLLM 拆 → 引入 `ExecuteModelState` 暂存 + `return None` 约定 / MindIE 不拆（async 路径靠 forward_thread 后台线程并发） / SGLang 可拆可不拆（`delay_sample_func` 仅在 grammar+overlap+非spec 时启用）。vLLM 的拆分是最复杂的设计，但也是最灵活的——原生支持"step 1 sample 与 step 2 forward 并发"的 fine-grained pipeline。
4. **零拷贝 IPC 仅 vLLM 有**：vLLM `MessageQueue` 共享内存环形 buffer + 直接 deserialize（[multiproc-ipc.md](../vllm/topics/multiproc-ipc.md)）/ SGLang 用 ZMQ + `recv_pyobj/send_pyobj` pickle（[TokenizerManager.md CONTRADICTION](../sglang/entities/TokenizerManager.md) 已标"吞吐 ceiling 受 pickle 性能影响"）/ MindIE 同进程无需 IPC。
5. **draft model runner 持有位置差异（spec decode）**：**SGLang `TpModelWorker.model_runner_list: List[ModelRunner]`**（worker 层多个）/ **MindIE `MtpWorker.draft_model_runner`**（worker 层双字段）/ **vLLM `GPUModelRunner.drafter`**（model runner 层嵌套）—— SGLang/MindIE 在 worker 层共享 vs vLLM 在 model runner 层嵌套。

**§9 综合对照 cheat sheet 关键行**

| 维度 | MindIE | vLLM | SGLang |
|---|---|---|---|
| Executor 抽象 | ❌ 无 | ✅ 4 实现（UniProc/MultiProc/Ray/RayV2） | ❌ 无 |
| Worker 主类 | 无（PluginManager.forward_thread）| `Worker(WorkerBase)` | `TpModelWorker(BaseTpWorker)` |
| Worker 进程独立 | ❌ | ✅（MP/Ray） | ❌ |
| 跨进程 RPC 协议 | N/A | MessageQueue + `getattr(self.worker, method)` | N/A |
| 零拷贝 IPC | N/A | ✅ MessageQueue 共享内存 | ❌（pickle）|
| forward+sample 拆分 | ❌ 同 method | ✅ 拆为两 method | ⚠️ 同 method + 可选 closure |
| EPLB / Elastic-EP | ❌ | ✅ | ✅ |
| Worker 健康监控 | N/A | `monitor_workers` 线程 | `SubprocessWatchdog` |
| 抽象层级数量 | **3** | **6** | **4** |

**Anchor-driven cross-check（§10）**

| Anchor | MindIE 命中 | SGLang 命中 |
|---|---|---|
| vLLM `Executor` 4 实现工厂 | C++ `src/executor/executor.cpp` 有 `Executor` 类但**含义不同**（仅 connector 进程编排，非分布式抽象）→ N/A | `class Executor` 全树 **0 命中** → N/A |
| vLLM `getattr(self.worker, method)` 字符串 RPC | `getattr(self\.worker` / `MessageQueue.*enqueue` 全树 **0 命中** | **0 命中**（用 ZMQ pickle 直传对象，不用方法名 RPC） |
| SGLang `TpModelWorker.model_runner_list: List[ModelRunner]` MTP 多 ModelRunner | MindIE `MtpWorker.draft_model_runner`（双字段，**同语义** worker 层共享） | vLLM `GPUModelRunner.drafter`（**结构差异**：model runner 层嵌套） |

**协同更新**

| 文件 | 修改 |
|---|---|
| [comparison/dimensions.md](../comparison/dimensions.md) §dim-executor | 末尾"专项 executor-worker.md TODO"改为"DONE 链接到 executor-worker.md"，加 11 子维度摘要 |
| [comparison/index.md](../comparison/index.md) | (a) frontmatter `related:` 加 executor-worker.md；(b) 待建表"Executor / Worker"行 TODO → DONE；(c) "已建对比页"表新增 Executor / Worker 行 |
| [wiki/index.md](../index.md) | 跨项目对比表"Executor / Worker"行 TODO → DONE |

**Markers added**

| 类型 | 数量 | 详情 |
|---|---|---|
| `[!todo] VERIFY` | **3** | (1) MindIE C++ `src/executor/executor.cpp` 中 `Executor` 类的真实角色 / (2) SGLang `BaseTpWorker` 基类的接口契约（abstract method 清单） / (3) vLLM `RayDistributedExecutor` V2 与 V1 `RayExecutor` 的差异 |
| `[!warning] CONTRADICTION` | **2** | (1) §1 抽象层级数量"vLLM 6 层"是 synthesis 而非源码原话 / (2) §5 "vLLM forward+sample 拆分原因 = async scheduler 必须"是 synthesis 推断；源码注释只提 `execute_model_state` 必须为空的断言 |
| §10 anchor-driven N/A 强论断 | **5** | 见上表（3 anchor × 三方反向扫）|

**净变化**

| | before | after |
|---|---|---|
| comparison topics DONE 数 | 11/24 | **12/24**（+ executor-worker） |
| `wiki/index.md` 跨项目对比表 DONE 行 | 10 | **11** |
| dimensions.md §dim-executor 深度对比链接 | engine-architecture.md §5（轻量） | **executor-worker.md（深度，11 子维度）+ engine-architecture.md §5（轻量）双层** |
| 总 .md 文件数 | 70 | **71**（+1） |

**Lessons learned**

1. **"父对比页 §5 铺种子 → 子对比页深化"是良性循环**（再次印证）：本轮 executor-worker.md 直接基于刚 ingest 完的 [engine-architecture.md §5 Engine↔Worker 边界](../comparison/topics/engine-architecture.md) 5 维对照表，并扩展为 11 子维度独立页。**父子对比页之间通过"轻量 §X"指向"深度专题页"的相互回链是可推广的 wiki 结构模式**——避免单页超过 50KB 同时保留"自顶向下入门"的导航。
2. **"Worker 命名陷阱"是跨项目对比中第二重要的 synthesis**（仅次于"Engine 命名陷阱"）：vLLM `Worker` 是进程级 + 设备级包装；MindIE 没有独立 Worker 类（PluginManager.forward_thread 充当）；SGLang `TpModelWorker` 是对象级包装。在不同上下文里说"worker"可能指完全不同的实体——本页 §1 / §2 / §3 都需要在表头明示这点。
3. **抽象层级数量是个高价值 synthesis 但易引入争议**：本轮把 vLLM 标为 "6 层" 是按内部实现路径计的最深值；如果按"用户 API 视角"只算 2-4 层。这种 synthesis 容易引起误读，必须在 CONTRADICTION 中明示其计算口径。
4. **anchor-driven cross-check 第 3 个 anchor 揭示了"draft model runner 持有位置"这个 spec decode 设计差异**：worker 层共享（SGLang/MindIE）vs model runner 层嵌套（vLLM）—— 这是从 worker 视角才能发现的差异，单看 [comparison/topics/speculative-decoding.md](../comparison/topics/speculative-decoding.md) 可能漏。**跨页 anchor cross-check 经常带来"原本不知道自己不知道"的发现**。
5. **forward+sample 拆分原因的 synthesis 依据弱**：[gpu_model_runner.py:3767-3771](d:\design\vllm\vllm\v1\worker\gpu_model_runner.py) 的注释只断言 `execute_model_state must be None`，没明示"为 async scheduler 而拆"。本页 §5 给出了 synthesis 但加了 CONTRADICTION 说明。**未来 verify pass 时应优先 grep `docs/design/` 看官方设计文档对该拆分的解释**。
6. **多 ModelRunner 字段名差异反映 spec decode 框架"借壳"程度**：SGLang `model_runner_list` 复用 worker 框架（最通用）/ MindIE `MtpWorker.draft_model_runner` 双字段（限定 main+draft）/ vLLM `drafter` 在 ModelRunner 内（最深嵌入）。这种"字段名 → 设计哲学"的映射是 cross compare 中具体可观测的"设计意图"指标。

**当前 wiki 状态**

| 项目 | overview | index | modules | entities | topics | 总数 |
|---|---|---|---|---|---|---|
| MindIE-LLM | ✅ | ✅ | 3/14 | 9/9 | 8/13 | 20 |
| vLLM | ✅ | ✅ | 2/26 | 10/17 | 5/14 | 17 |
| SGLang | ✅ | ✅ | 3/30 | 2/11 | 2/14 | 7 |
| comparison | — | ✅ | — | — | **12/24**（+ executor-worker） | **12 + dimensions** |

**总 .md 文件数**：71（+1 新建）。

**下一步建议**

按 ROI 排序：

1. **`compare multiproc-ipc across all`** —— 本轮 §3 / §4 + engine-architecture.md §1 / §6 已铺好种子（vLLM ZMQ + MessageQueue shm 双 IPC + cloudpickle / SGLang 全 ZMQ pickle / MindIE shm + protobuf），可直接深化到 6-8 子维度
2. **`ingest sglang entities P0`**（`Engine` + `TpModelWorker` 拆分页 + `DataParallelController` + `SchedulePolicy`）—— SGLang entity 端是三家中最少的（仅 2/11），最大欠债；本轮 worker 对比已确认 SGLang `TpModelWorker` 与 MindIE `MtpWorker` / vLLM `Worker` 是对偶关系，详细 ingest ROI 高
3. **`verify vllm/entities/AsyncLLM.md` / `LLMEngine.md`** —— 剩余 P0 entity markers 消化
4. **`verify comparison/topics/engine-architecture.md`** —— 5 markers 中 2 个 CONTRADICTION 涉及 "synthesis 类比 vs 源码原话" 的边界澄清
5. **被动等待**：三仓 HEAD 都未动，无需触发 §12 增量

---

## [2026-04-18] compare | cross | engine-architecture across all 3 projects

**触发**：用户 `compare engine-architecture across all`，承接刚完成的 [vllm entities P0+P1 ingest](#) + 双 verify pass，以及 [comparison/index.md:51](../comparison/index.md) 长期标 TODO 的"Engine 架构"主题。

**新建页**：[comparison/topics/engine-architecture.md](../comparison/topics/engine-architecture.md)（约 530 行 / ~28 KB），`status=verified`、`confidence=high`、`type=comparison`、`project=cross`。覆盖 13 个子维度。

**13 子维度**（详见对比页正文）

1. 顶层进程拓扑（process model，含三家 mermaid + 3 方对照表：进程数、IPC、跨语言）
2. 顶层 Engine 入口对象（含"engine 命名陷阱"synthesis：MindIE/vLLM/SGLang `Engine` 字面所指完全不同）
3. Sync vs Async API 拆分（**MindIE 单类 + flag** vs **vLLM 双类 LLMEngine/AsyncLLM** vs **SGLang 同类全 async**）
4. Engine ↔ Scheduler 边界（Scheduler 实现语言 / 进程位置 / 调用方式 / 请求注入路径 / 输出回流 / 多 scheduler 协调）
5. Engine ↔ Worker 边界（Executor 抽象层 / Worker 是否独立进程 / 调用机制 / 多 worker 并行 / PP 实现）
6. Tokenize / Detokenize 进程位置（**SGLang 是唯一独立 detokenize 进程**的 N/A 强论断对比）
7. 跨语言（C++ / Python）边界（**MindIE 是唯一在 engine 路径上 C++/Python 紧耦合**的）
8. 启动序列（MindIE Generator.__init__ 11 步 / vLLM LLMEngine.__init__ / SGLang Engine.__init__ + _launch_subprocesses；含启动复杂度对照表）
9. 关闭语义（shutdown / `__del__` / atexit / weakref.finalize）
10. PD 分离与 Engine 关系（**MindIE 内置 PDInterface** vs **vLLM/SGLang 外挂**）
11. 综合架构对比表（顶层 cheat sheet，14 行覆盖所有关键差异）
12. Anchor-driven cross-check（3 个 anchor × 三方反向扫，**5 处 N/A 强论断**）
13. 与 PD 优化的关联（synthesis：5 条可借鉴的优化思路）

**关键 synthesis（综合三家差异）**

1. **MindIE = Python `Generator` + C++ `LlmEngine` 双层架构**：Python 顶层装配，C++ 跑主调度循环，反向调 Python `PluginManager.generate_token` 执行 forward；同进程，无 ZMQ；C++↔Python pybind 紧耦合。
2. **vLLM = 三层 Python 洋葱**（`LLMEngine`/`AsyncLLM` → `EngineCoreClient` → `EngineCore`/`EngineCoreProc`）+ **可插拔 4 实现的 `Executor` 抽象**；进程模型从 1 进程到 1+1+N 进程到 Ray actor 完全可配。
3. **SGLang = 严格 3 进程 ZMQ pipeline**：`TokenizerManager`（主）→ `Scheduler` 子进程（直接持 `TpModelWorker`）→ `DetokenizerManager` 子进程；不可降为 inproc。
4. **"Engine" 命名陷阱**：MindIE `LlmEngine` 是 C++ 调度循环；vLLM `LLMEngine` 是 sync 顶层入口；SGLang `Engine` 是 launcher（fork 三进程后立即返回）—— **类名不可直接类比**。
5. **Worker 是独立进程吗？**：vLLM **是**（MultiprocExecutor 时）；MindIE/SGLang **否**（同进程后台线程或 scheduler 内对象）—— vLLM 的 worker 进程模型是三家中最灵活的。
6. **Detokenize 是独立进程吗？**：SGLang **是**（独占设计选择）；MindIE/vLLM **否**（同进程） —— SGLang 的 manager-pipeline 哲学特征点。
7. **Sync vs Async 形态差异**：MindIE 同类 + `async_inference` flag（运行时开关）；vLLM 双类（`LLMEngine` vs `AsyncLLM`，类型分离）；SGLang 同类全 `async def`，sync 用户必须 `asyncio.run` 包裹。三家 async 语义完全不同（详对比页 §3）。

**§13 综合 cheat sheet 关键行**

| 维度 | MindIE | vLLM | SGLang |
|---|---|---|---|
| 进程数（最小） | 1 | 1（inproc） | **3** |
| 进程数（DP4 TP8） | 4 + connector | **37** | **34** |
| Scheduler 实现 | **C++** | Python | Python |
| Executor 抽象层 | ❌ 无 | ✅ 4 实现 | ❌ 无 |
| Worker 独立进程 | ❌ | ✅（MP/Ray 时） | ❌ |
| Detokenize 进程 | 同 generator | 同前端 | **独立子进程** |
| C++ engine 主循环 | ✅ | ❌ | ❌ |
| PD 在 Engine 内 | ✅ 内置 PDInterface | ❌ KVConnector 外挂 | ❌ disagg_service 外挂 |

**Anchor-driven cross-check 结果（§12，按 [AGENTS.md §8 规则 6](../AGENTS.md)）**

| Anchor | MindIE 命中 | SGLang 命中 |
|---|---|---|
| vLLM `EngineCoreClient.make_client` 6 子类工厂 | **0 命中** | **0 命中** —— N/A：MindIE/SGLang 都没有"客户端选型"概念（前者无跨进程边界，后者强制 3 进程） |
| MindIE `Generator.async_inference` flag | vLLM **0 命中**（用类型分离 `LLMEngine` vs `AsyncLLM` 而非 flag） | SGLang **0 命中**（用 `disable_overlap_schedule` server arg + dispatch_event_loop 路由） |
| SGLang `DetokenizerManager` 独立子进程 | MindIE **0 命中** —— detokenize 在 PluginManager 内同进程 | vLLM **0 命中** —— detokenize 在 OutputProcessor 内同进程 |

**协同更新**

| 文件 | 修改 |
|---|---|
| [comparison/dimensions.md](../comparison/dimensions.md) | §dim-overall / §dim-engine / §dim-executor 三维度 cell 全部升级为深度 anchor + 加深度链接到 engine-architecture.md |
| [comparison/index.md](../comparison/index.md) | (a) frontmatter `related:` 加 engine-architecture.md + speculative-decoding.md；(b) 待建表"Engine 架构"行 TODO → DONE；(c)"已建对比页"表新增 Engine Architecture 行 |
| [wiki/index.md](../index.md) | 跨项目对比表"整体架构" / "Engine 抽象" 双行 TODO → DONE；Executor / Worker 加种子链接 |

**净变化**

| | before | after |
|---|---|---|
| comparison topics DONE 数 | 10/24 | **11/24**（+ engine-architecture）|
| `wiki/index.md` 跨项目对比表 DONE 行 | 8 行 | **10 行**（+ "整体架构" + "Engine 抽象"）|
| dimensions.md 三维度（§dim-overall/§dim-engine/§dim-executor）深度对比链接 | 0/3 | **3/3** 全部加上 |
| 总 .md 文件数 | 69 | **70**（+1） |

**Lessons learned**

1. **"先 ingest 完两家相关 entity 再做 cross compare"= 极高 ROI 模式**：本轮直接基于 [刚 ingest 的 6 个 vLLM P0+P1 entity](#) + 已有 9 个 MindIE entity + SGLang `TokenizerManager`/`Scheduler` 现成页面综合，未启动任何额外 subagent，全部 anchor 来自现有 wiki + 少量补 grep（SGLang `Engine.__init__` + `_launch_subprocesses`），耗时短而信息密度极高。这是 [§AGENTS.md §6 query 工作流](../AGENTS.md) "先 grep wiki 不够再读源码" 的最佳实践案例。
2. **"Engine 命名陷阱" 是跨项目对比中最重要的 synthesis**：MindIE/vLLM/SGLang 都有 `Engine` 字面但所指完全不同（C++ 调度循环 / sync 顶层入口 / 进程 launcher）。**对比页第一段必须显式破除这种类名同指假设**，否则未来 reader 会带着错误前提阅读。这条 lesson 可推广到任何"同名异义"概念（如 `Worker` / `Scheduler` / `Executor` 也存在类似问题）。
3. **3 anchor × 三方反向扫 = 5 处 N/A 强论断**：本轮 §12 anchor-driven cross-check 没发现新 entity（已都 ingest），但发现的**全是 N/A 强论断**——这些"X 有 / Y 没有"的差异本身就是设计哲学的关键证据。**N/A 强论断的累积是跨家 wiki 价值的核心来源之一**。
4. **同维度可分多页递进深化**：本轮覆盖 §dim-overall / §dim-engine / §dim-executor 三个 dimensions 维度；其中 §dim-executor 的"Executor / Worker 模型"还可以单独建 [comparison/topics/executor-worker.md](../comparison/topics/executor-worker.md)（已铺种子）做更深 6-8 子维度展开。这种"父页统揽 + 子页深化"是 wiki 自然演化方向。
5. **跨项目对比的 mermaid 多图模式**：本轮三家进程拓扑各画一张 mermaid，对照表显示进程数差异。这种"每家一张图 + 一张对照表"模式比"一张大图试图叠所有家"更易读，建议未来 cross compare 默认采用。
6. **PD 优化借鉴 synthesis（§13）是 cross compare 的产出价值最大化**：跨家对比页本质是给"产品/优化决策者"的输入，§13 给出 5 条具体可借鉴的优化思路，让对比页跳出"中立陈述"框架，提供实操指引。

---

