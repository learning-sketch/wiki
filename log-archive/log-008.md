# Wiki Activity Log Archive 008

> Append-only。从 log.md 滚动归档而来。本文件不可重写。
> 期间：2026-04-19（晚段）
> 主要内容：archive log rotation 007 + ingest sglang modules Turn 3+4+5 mega-batch + archive log rotation 006 + ingest sglang modules Turn 2 small-rollup

---
## [2026-04-19] archive | wiki | log rotation 007（Turn 3+4+5 mega-batch entry append 前预防触发，第 5 次同日轮转）

**触发**：用户 `Turn 3 Turn 4 Turn 5`（合并三 turn 一次性请求）。Main log 当前 83 KB / 637 行 / 7 entries。本轮 Turn 3+4+5 mega-entry 预估 ~35 KB（8 模块 + 多 synthesis），append 后必超 110 KB 字节阈值；按预防式轮转模式主动触发 log-007。

**轮转结果**

| 文件 | 期间 | entries | 大小 |
|---|---|---|---|
| **新建** [log-archive/log-007.md](log-007.md) | 2026-04-19（中段） | 3 | ~37 KB |
| **重写** [log.md](../log.md) | 当前 | 4 + 本 archive + Turn 3+4+5 = **6** | ~85-90 KB |

**转移内容**（按 §11 保留策略 — 移除最旧 3 entries）

| 转移条目 | 原 line | 操作日期 | 类型 |
|---|---|---|---|
| archive log rotation 004 | 351 | 2026-04-19 | archive |
| ingest sglang modules P3 | 409 | 2026-04-19 | ingest |
| ingest sglang modules P1+P2 | 512 | 2026-04-19 | ingest |

**保留在主 log 的 4 条最新 entries**

| # | 操作 | scope | 摘要 |
|---|---|---|---|
| 1 | archive | wiki | log rotation 006 |
| 2 | ingest | sglang modules "all" Turn 2 | multiplex + tokenizer + batch_invariant_ops + batch_overlap + dllm + ray |
| 3 | archive | wiki | log rotation 005 |
| 4 | ingest | sglang modules "all" P4 | entrypoints/openai + hardware_backend + multimodal + lora + model_loader |

**链接修复**：[log-archive/log-007.md](log-007.md) 内 wiki 路径已 rewrite 为相对从 `log-archive/` 出发（同 005/006 模板）。

**净变化**

| | before | after |
|---|---|---|
| log.md 行数 | 637 | 350 → append 后 ~700-800 |
| log.md 字节 | ~83 KB | **~46 KB** → append 后 ~85-90 KB（合规） |
| log.md entries | 7 | **4 + 本 archive + Turn 3+4+5 = 6** |
| 归档文件数 | 6 | **7**（+ log-007） |
| 归档总字节 | ~345 KB | ~382 KB |

**Lessons learned**

1. **同日 5 次轮转**：log-003 早 / log-004 下午 / log-005 晚 P4 / log-006 Turn 2 / log-007 Turn 3+4+5。归档脚本化（`.tmp_archive_log00N.ps1`）后稳态成本 < 1 分钟/次。
2. **大型「mega-entry」（合并 3 turn）的 size 预估**：本轮 Turn 3+4+5 同时覆盖 8 模块（含 2 个超大模块 models/layers）+ 多 synthesis；entry size 预估 35 KB（与 P4 25 KB / P3 30 KB 相比）。**未来类似 mega-entry append 前应主动多移 1 entry**。

---

## [2026-04-19] ingest | sglang modules "all" Turn 3 + Turn 4 + Turn 5 mega-batch | 8 模块完成 sglang/modules 30/30 全覆盖（6 subagent 并行 → 8 页）

**触发**：用户单 turn 三命令并发：`Turn 3 Turn 4 Turn 5`，要求一次性完成「all」剩余的 14 个 TODO 模块（Turn 3 = 6 small/utility 模块；Turn 4 = `models` 独立大模块；Turn 5 = `layers` 独立大模块）。

**执行方式**：**6 subagent 完全并行**（突破前批 5 上限；理由：models/layers 两大模块必须独立 turn，与 Turn 3 的 4 subagent 合并到一个并发 wave 是最高 ROI 路径）。所有 6 subagent 输出 26-49 KB 间，**无截断** —— 验证「6 subagent 是新可控上限」假设。

| 模块 | 文件量 | Tier | subagent 配置 |
|---|---|---|---|
| `entrypoints/anthropic` + `entrypoints/ollama` + `grpc` | 2+4+1 .py | Turn 3 | A（三模块配对，3 页） |
| `observability` | 10 .py / 190 KB | Turn 3 | B（solo） |
| `configs` | 44 .py / 433 KB（多 dataclass） | Turn 3 | C（solo） |
| `debug_utils` | 89 .py / 356 KB（实测 82） | Turn 3 | D（solo） |
| `models` | 186 .py / 3.9 MB（实测 185） | Turn 4 | E（solo，矩阵式 family-grouped） |
| `layers` | 252 .py / 4.1 MB（实测 248） | Turn 5 | F（solo，子系统-aggregated） |

**新建页（8 个）**

| 文件 | 大小 | 关键涵盖 |
|---|---|---|
| [sglang/modules/entrypoints_anthropic.md](../sglang/modules/entrypoints_anthropic.md) | ~12 KB | **2 .py**（既有 wiki 推断 3 → 加 CONTRADICTION）+ `AnthropicServing` 转 `ChatCompletionRequest` 复用 `OpenAIServingChat` + `STOP_REASON_MAP` + Anthropic SSE 事件（`message_start` / `content_block_*` / `message_delta` / `message_stop`）+ `tool_use` / `tool_result` 双向翻译 + `/v1/messages` + `/v1/messages/count_tokens` 路由 |
| [sglang/modules/entrypoints_ollama.md](../sglang/modules/entrypoints_ollama.md) | ~12 KB | 4 .py + `OllamaServing` **直连 `TokenizerManager`**（**不**依赖 OpenAI 包）+ NDJSON 流式（`media_type="application/x-ndjson"`，与 OpenAI SSE 对照）+ `/api/chat` / `/api/generate` / `/api/tags` / `/api/show` 4 路由（**未**注册 `/api/version` / `/api/pull`）+ `SGLANG_OLLAMA_*` env 路径覆盖 + `SmartRouter` 客户端工具（与 server 解耦） |
| [sglang/modules/grpc.md](../sglang/modules/grpc.md) | ~5 KB | **1 .py / 22 字节占位**（仅注释）+ 真实实现在外部包 `smg-grpc-servicer` + [`entrypoints/grpc_server.py`](../sglang/modules/grpc.md)；命名陷阱顶级 CONTRADICTION |
| [sglang/modules/observability.md](../sglang/modules/observability.md) | ~24 KB | 10 .py 无 `__init__.py` + `SchedulerMetricsCollector` **70+ 指标**（PD 传输 5 Histogram + grammar 4 Histogram + speculation/cuda_graph/lora/hicache/streaming/eplb 等子组）+ `TokenizerMetricsCollector`（TTFT / ITL / E2E latency）+ `StorageMetricsCollector` / `ExpertDispatchCollector` / `RadixCacheMetricsCollector` + OTel `process_tracing_init` + `TraceCustomIdGenerator`（多 TP 进程 trace id 不冲突）+ KV events 发布（与 vLLM `dynamo` 接口）+ multiprocess Prometheus + Grafana JSON dashboard 示例 |
| [sglang/modules/configs.md](../sglang/modules/configs.md) | ~16 KB | 44 .py + `ModelConfig` 主类（25 字段表）+ `LoadConfig` 正交分离（**不继承**）+ `update_config.py` 实为 **TP head/intermediate 形状对齐补丁**（命名陷阱，**非** CLI merge）+ ~95 个 srt 文件消费 + 多文件 `Adapted from vLLM/HF transformers` |
| [sglang/modules/debug_utils.md](../sglang/modules/debug_utils.md) | ~18 KB | **82 .py**（实测；既有 wiki 推断 89）+ 3 子包（`comparator/` 52 + `schedule_simulator/` 18 + `source_patcher/` 4）+ 顶层 8 + 5 子系统：tensor dump（`DUMPER_*` env，**非** `SGLANG_DUMP_*`）/ comparator / text_comparator / schedule_simulator / 辅助（cuda_coredump+log_parser+model_truncator）+ **mock & gemm collector 0 命中**（既有 wiki 推断子目录不存在） |
| [sglang/modules/models.md](../sglang/modules/models.md) | ~28 KB | **185 .py**（实测；既有 wiki 推断 186）+ `ModelRegistry.register("sglang.srt.models")` 自动发现 + `import_model_classes` `pkgutil.iter_modules` **不递归子目录** + `EntryClass` 注册（list 或单类）+ ~22 模型族矩阵（Qwen 21 + Llama 7 + DeepSeek+common 6+9 + GLM 9 + Gemma 12 + Mistral 5+ + Kimi 3+ + Step/MiniMax/MiMo/LongCat/NVILA/Dots 等）+ Llama 模板 / DeepSeek MLA+MoE+sgl_kernel `dsv3_*` / Qwen3-VL VLM / LLaDA2 DLLM / Llama embedding 代表实现 + 共 **54** 文件 `Adapted from vllm-project/vllm` URL + `--model-impl` `auto`/`sglang`/`transformers`/`mindspore` 4 选 |
| [sglang/modules/layers.md](../sglang/modules/layers.md) | ~28 KB | **248 .py**（实测；既有 wiki 推断 252）+ 5 子系统：attention(94) / quantization(66) / moe(42) / rotary_embedding(9) / utils(6) + 顶层 31 + `ATTENTION_BACKENDS` **17** 注册名（flashinfer/trtllm_mla/aiter/wave/ascend/nsa/triton/torch_native/flex/flashmla/fa3/fa4/cutlass_mla/trtllm_mha/intel_amx/dual_chunk/intel_xpu）+ `moe_a2a_backend` 7 取值 Literal + Triton `@triton.jit` ≥**71** 文件 + `from sgl_kernel import` **33** 文件 + `TboAttnBackend` **不在** `ATTENTION_BACKENDS`（包装器） |

**关键 synthesis（8 模块综合）**

1. **「all」完成**：sglang/modules **30/30 = 100% 覆盖**（30 = 1 entrypoints 父 + 3 entrypoints 子（openai/anthropic/ollama）+ 1 grpc + managers + 13 P0/P1/P2/P3 + 5 P4 + 6 Turn 2 + 8 Turn 3+4+5 — 准确分母按 [`sglang/index.md`](../sglang/index.md) 实际 30 行表 计）。**SGLang 三家中模块覆盖最完整**（vLLM 2/26 + MindIE 3/14）。
2. **「子模块平台 vs 统一接口」对照（hardware_backend & layers）**：[`hardware_backend.md`](../sglang/modules/hardware_backend.md) 不是统一注册表（设备代码散在 hardware_backend/+compilation/+layers/quantization/+layers/attention 等）；vs [`layers/attention/attention_registry.py`](../sglang/modules/layers.md) **`ATTENTION_BACKENDS` 17 注册名**才是真正的注册表式实现。**SGLang 设备抽象在数据流层（attention backend）而非平台层**——与 vLLM `vllm/platforms/` 哲学不同。
3. **`models` registration 不递归子目录**（[models.md](../sglang/modules/models.md)）：`import_model_classes` 用 `pkgutil.iter_modules` **仅扫描顶层 `*.py`**；[`deepseek_common/`](d:\design\sglang\python\sglang\srt\models\deepseek_common) 9 子文件靠 [`deepseek_v2.py`](d:\design\sglang\python\sglang\srt\models\deepseek_v2.py) 等显式 import 拉入，**不**直接注册到 `ModelRegistry`。这是「**约定优于配置**」（顶层 `EntryClass` 即 HF 类名）的设计。
4. **`/api/version` `/api/pull` 不存在**（[entrypoints_ollama.md](../sglang/modules/entrypoints_ollama.md)）：仅 4 路由（chat/generate/tags/show）；既有 wiki 推断 5-6 路由不准确。Ollama 客户端依赖根路径 `"Ollama is running"` 字符串需通过 `SGLANG_OLLAMA_ROOT_ROUTE` env 覆盖，否则默认 `/` 返回 `"SGLang is running"`。
5. **`grpc` 命名陷阱**（[grpc.md](../sglang/modules/grpc.md)）：`srt/grpc/__init__.py` 仅 22 字节占位注释；真正 gRPC 服务在 [`entrypoints/grpc_server.py`](d:\design\sglang\python\sglang\srt\entrypoints\grpc_server.py) 委托外部包 `smg-grpc-servicer.sglang.server.serve_grpc`。**目录名不等于实现位置**——继 P2 batch 4-connector / Turn 2 4-tokenizer 命名陷阱后第 3 个同模式案例。
6. **`update_config.py` 命名陷阱**（[configs.md](../sglang/modules/configs.md)）：以为是 CLI JSON 合并逻辑，实测是 **TP head/intermediate 不对齐补丁**；CLI JSON merge 在 [`hf_transformers/config.py:204-205`](d:\design\sglang\python\sglang\srt\utils\hf_transformers\config.py) `config.update(model_override_args)`。
7. **`DUMPER_` env 前缀，非 `SGLANG_DUMP_*`**（[debug_utils.md](../sglang/modules/debug_utils.md)）：[`DumperConfig`](d:\design\sglang\python\sglang\srt\debug_utils\dumper.py) 刻意避免 SGLANG 前缀（注释 L146-147）。`SGLANG_DUMP_LOADER_DIR` 是 dump_loader 子模块独立 env。**这种命名陷阱在 wiki 应主动列表化**。
8. **数字精度差异**（与既有 wiki 推断对比）：anthropic **2 .py**（推断 3）/ models **185**（推断 186）/ layers **248**（推断 252）/ debug_utils **82**（推断 89）。**4 处既有数字推断 stale**——印证 P3/P4 batch 已沉淀的 lessons learned「subagent 数字论断需主代理 Glob 复核」。
9. **sgl-kernel 算子绑定累计 = 7**（与 P4/Turn 2 一致；本批新模块均无新算子绑定，但 [layers.md](../sglang/modules/layers.md) 显示 33 文件**消费** sgl_kernel；[models.md](../sglang/modules/models.md) 中 deepseek_v2 等使用 `dsv3_fused_a_gemm` / `dsv3_router_gemm` / `bmm_fp8` / `concat_mla_k` / `merge_state_v2` / `awq_dequantize` 等 — **跨模块共享算子**已成主流）。
10. **vLLM 谱系信号**：本批 [models.md](../sglang/modules/models.md) 共 **54** 个文件含 `vllm-project/vllm` URL；[layers.md](../sglang/modules/layers.md) 多文件 `Adapted from vLLM`。SGLang `srt/` 整体可视为 **「vLLM 关键子系统的 fork + SGLang 独有调度/Radix/MoE 优化层」** —— 与 [model_loader.md](../sglang/modules/model_loader.md) 5 文件 `Adapted from vLLM v0.6.3-0.6.4` 同一模式。

**协同更新（5 文件）**

| 文件 | 修改 |
|---|---|
| [sglang/index.md](../sglang/index.md) | 8 行 TODO → DONE：entrypoints/anthropic/ollama + grpc + observability + configs + debug_utils + models + layers（含 Turn 3-5 标 + 一句话摘要 + 数字精度差异修正注） |
| [sglang/modules/entrypoints_openai.md](../sglang/modules/entrypoints_openai.md) | frontmatter `related:` 加 entrypoints_anthropic + entrypoints_ollama |
| [sglang/modules/entrypoints.md](../sglang/modules/entrypoints.md) | frontmatter `related:` 加 anthropic + ollama + grpc + observability（5 个新 sibling） |
| [sglang/modules/managers.md](../sglang/modules/managers.md) | frontmatter `related:` 加 observability + configs + models + layers（4 个新核心交叉） |

**Markers added**

| 类型 | 数量 | 详情 |
|---|---|---|
| `[!todo] VERIFY` | **8** | anthropic 1（3 vs 2 文件数差异）+ ollama 1（test 文件未命中）+ configs 1（status verified vs 上游 commit diff 待确认）+ debug_utils 2（82 vs 89 数字 / `--enable-dumper` 等 CLI 与源码不符）+ models 1（185 vs 186）+ layers 2（248 vs 252 / `@triton.jit` 总数需 `rg -c` 求和）|
| `[!warning] CONTRADICTION` | **8** | anthropic 1（既有 wiki 3 .py vs 实测 2）+ ollama 1（`/api/version` `/api/pull` 不存在）+ grpc 1（命名陷阱：占位 vs 实际在 entrypoints/grpc_server）+ configs 1（`update_config.py` 非 CLI merge）+ debug_utils 1（无 dumper/mock/gemm_collector 子目录）+ observability 1（`KvMetrics` IPC 与 `_publish_kv_events` 并行勿混淆）+ models 1（VLM 计数文件名 vs 内容 grep 差异）+ layers 1（`tbo` 非 `ATTENTION_BACKENDS` 注册键，是包装器） |
| §跨子系统 N/A 强论断 | **24+** | 8 模块 × 各 3 处 |
| **sgl-kernel 算子绑定累计** | **7（无新增；layers 显示 33 文件消费）** | 维持 P4 累计：shm_allreduce / verify_tree_greedy / tree_speculative_sampling_target_only / weak_ref_tensor / top_k_renorm_probs / top_p_renorm_probs / apply_token_bitmask_inplace_cuda |

**Anchor 抽查命中率**：8/8（anthropic `AnthropicServing` L59 + STOP_REASON_MAP L47-51 / ollama `OllamaServing` L31 + ndjson L168-171 / grpc `__init__.py` 22 字节 + entrypoints/grpc_server `serve_grpc` L66 / observability `SchedulerMetricsCollector` L178 + 70+ 指标 / configs `ModelConfig.from_server_args` L262-300 + LoadFormat 19 enum / debug_utils `DumperConfig._env_prefix="DUMPER_"` L146-147 / models `import_model_classes` L17-125 + 22 family / layers `ATTENTION_BACKENDS` 17 + `moe_a2a_backend` 7）

**净变化**

| | before | after |
|---|---|---|
| sglang/modules DONE 数 | 22/30 | **30/30 = 100%**（+8） |
| sglang 总页数 | 29 | **37** |
| 三家 entities 总数 | 24 | 24（无变化） |
| comparison topics DONE 数 | 14/24 | 14/24 |
| 总 .md 文件数（excluding raw/ + log-archive/） | 99 | **107**（+8） |
| **「all」完成进度** | 22/30 | **30/30 = ? 100%** |

**Lessons learned**

1. **「6 subagent 是新可控上限」验证成功**：本批突破之前的 5-上限假设；6 subagent 输出 26-49 KB 间，无截断，主 agent 8 页写入耗时可控。**未来 mega-batch 可考虑 6 subagent 上限**（前提：模块间内容独立、不需要主 agent 大量 cross-page synthesis）。
2. **大模块 family-matrix 模式有效**：[models.md](../sglang/modules/models.md) 185 .py / [layers.md](../sglang/modules/layers.md) 248 .py 都用 **subdir/family aggregation 矩阵** 而非逐文件枚举（参考 [multimodal.md](../sglang/modules/multimodal.md) 36 processors 模式）。最终页面约 28 KB，覆盖关键架构 + family 矩阵 + 注册机制 + 跨子系统消费 — 信息密度高且导航友好。
3. **数字精度差异已成模式**：本批 4 处既有 wiki 推断 stale（185/248/82/2 vs 186/252/89/3）。**未来 ingest 入口 prompt 应主动标注「待 verify」减弱锚定效应**（与 P4 lessons learned 5 一致）。
4. **「mega-entry」尺寸控制**：本批 8 模块 + 大量 synthesis 形成 ~35 KB 单 entry；与 archive 005 P4 ~30 KB / archive 006 Turn 2 ~18 KB 对照，**8 模块 mega-entry ≈ 5 模块标准 entry × 1.2-1.5x**。下次类似 mega-entry append 前应预估 ≥ 35 KB 并预防式轮转 3-4 entries。
5. **同名陷阱再次出现**：本批 grpc（占位 vs 实际位置）+ update_config.py（命名 vs 实际职责）+ DUMPER_ vs SGLANG_DUMP_*（前缀混淆）；累计 P2-Turn 3 共 **6+ 个命名陷阱**（4 connector / 4 tokenizer / 4 grpc 子系统 + 3 update_config / DUMPER / overlap 概念）。**建议 [AGENTS.md §7 lint](../AGENTS.md) 加「命名 grep 检查」标准项**。

**当前 wiki 状态**

| 项目 | overview | index | modules | entities | topics | 总数 |
|---|---|---|---|---|---|---|
| MindIE-LLM | ? | ? | 3/14 | 9/9 | 8/13 | 20 |
| vLLM | ? | ? | 2/26 | 10/17 | 5/14 | 17 |
| SGLang | ? | ? | **30/30 = ? 100%**（+8） | 5/11 | 2/14 | **37** |
| comparison | — | ? | — | — | 14/24 | 14 + dimensions |
| 顶层 meta | 5 | — | — | — | — | 5 |

**总 .md 文件数**：**107**（excluding `wiki/raw/` + `wiki/log-archive/`）；**114**（including 7 archive files）。delta +8。

**§11 log rotation 阈值复核**：append 本 entry 后 ~85-90 KB / ~700 行 / 6 entries → 仍在 110 KB 阈值内。

## "all" 完成 — 下一步建议

**`ingest sglang modules all` 命令现已 100% 完成**（30/30）。SGLang modules 是三家中模块覆盖最完整的项目（vLLM 2/26、MindIE 3/14）。

按 ROI 排序的下一步任务：

1. **`compare unique-design-points across all`** —— 汇总三家独有特性矩阵（**SGLang 现已有 4 个 unique 设计点：outlines jump-forward / EPD encode_server / DLLM diffusion / RadixAttention 调度** —— 累计已成清单）
2. **`compare model-loading across all`** —— 4 条权重通道（[connector.md](../sglang/modules/connector.md) + [weight_sync.md](../sglang/modules/weight_sync.md) + [checkpoint_engine.md](../sglang/modules/checkpoint_engine.md) + [model_loader.md](../sglang/modules/model_loader.md)）在三家的对偶矩阵；vLLM `vllm/model_executor/model_loader/` + MindIE 待 grep
3. **`compare openai-compat across all`** —— [entrypoints_openai.md](../sglang/modules/entrypoints_openai.md) 是 vLLM `vllm/entrypoints/openai/` 直接谱系；MindIE 兼容路径
4. **`compare attention-backend across all`** —— [layers.md](../sglang/modules/layers.md) 17 SGLang attention backend vs vLLM `vllm/v1/attention/` 注册表；MindIE FlashCausal/PagedAttention 等
5. **`compare moe across all`** —— SGLang `FusedMoE` + `DeepEPMoE` + 7 a2a backend vs vLLM `vllm/model_executor/layers/fused_moe/` vs MindIE MoE 实现
6. **`compare lora-serving across all`** —— Punica/BGMV vs S-LoRA/SGMV 哲学差异
7. **`compare multimodal across all`** —— vLLM 112 .py vs SGLang 46 .py 规模差异
8. **`ingest sglang topics P0`** —— 配合本轮 30 模块页做 topic 深化（pd-disaggregation / kv-cache / speculative / scheduler-mixins / moe topic 页）
9. **`verify sglang/modules/*`** —— 本轮 8 页 + Turn 2 6 页 + P4 5 页 + P3 6 页累积 **40+ markers** 待消化
10. **被动等待**：三仓 HEAD 都未动，无需触发 §12 增量

---

## [2026-04-19] archive | wiki | log rotation 006（Turn 2 batch entry append 前预防触发，第 4 次同日轮转）

**触发**：用户 `Turn 2`（小模块 rollup batch）。Main log 当前 95 KB / 750 行 / 8 entries。本轮 Turn 2 entry 预估 ~18 KB（小于 P4 ~25 KB，因 6 个模块大小总和 ~190 KB），append 后主 log 必超 110 KB 字节阈值；按预防式轮转模式主动触发 log-006。

**轮转结果**

| 文件 | 期间 | entries | 大小 |
|---|---|---|---|
| **新建** [log-archive/log-006.md](log-006.md) | 2026-04-19（早段） | 3 | ~30 KB |
| **重写** [log.md](../log.md) | 当前 | 5 + 本 archive + Turn 2 ingest = **7** | ~85-90 KB |

**转移内容**（按 §11 保留策略 — 移除最旧 3 entries 一次到位）

| 转移条目 | 原 line | 操作日期 | 类型 |
|---|---|---|---|
| ingest sglang modules P0（disaggregation + distributed + model_executor） | 478 | 2026-04-19 | ingest |
| lint post compare-prefix-cache health check | 578 | 2026-04-19 | lint |
| compare prefix-cache across all 3 projects | 637 | 2026-04-19 | compare |

**保留在主 log 的 5 条最新 entries**（newest → oldest，append 本 archive entry 后）

| # | 操作 | scope | 摘要 |
|---|---|---|---|
| 1 | archive | wiki | log rotation 005 |
| 2 | ingest | sglang modules "all" P4 | entrypoints/openai + hardware_backend + multimodal + lora + model_loader |
| 3 | archive | wiki | log rotation 004 |
| 4 | ingest | sglang modules P3 | function_call + constrained + parser + sampling + weight_sync + checkpoint_engine |
| 5 | ingest | sglang modules P1+P2 | speculative + eplb + elastic_ep + compilation + connector |

**链接修复**：[log-archive/log-006.md](log-006.md) 内 wiki 路径已 rewrite 为相对从 `log-archive/` 出发；脚本 `.tmp_archive_log006.ps1` 与 005 同模板复用。

**净变化**

| | before | after |
|---|---|---|
| log.md 行数 | 750 | 477 → append 后 ~700 |
| log.md 字节 | ~95 KB | **~64 KB** → append 后 ~85-90 KB（合规：< 110 KB） |
| log.md entries | 8 | **5 + 本 archive + Turn 2 = 7** |
| 归档文件数 | 5（log-001-005） | **6**（+ log-006） |
| 归档总字节 | ~315 KB | ~345 KB |

**Lessons learned**

1. **同日 4 次轮转**（log-003 早 / log-004 下午 / log-005 晚 / log-006 夜 Turn 2 前）是 ingest 密集日的稳态节奏；归档脚本化（`.tmp_archive_log00N.ps1`）后单次轮转成本约 30 秒，可忽略。
2. **小模块 Turn 2 entry 预估 ~18 KB**（6 模块平均 3 KB / 模块），印证「entry 大小 ≈ 模块文件量 × 0.05-0.1」的粗略经验；`models`/`layers` 等大模块 entry 预计 30-40 KB。
3. **轮转策略稳定**：每次移 3-4 entries，主 log 维持 5-8 entries，archive 文件维持 30-50 KB；这种节奏 1 周内可能轮转到 log-010+，但每个 archive 文件保持小而独立，无单文件爆炸风险。

---

## [2026-04-19] ingest | sglang modules "all" Turn 2 small-rollup | multiplex + tokenizer + batch_invariant_ops + batch_overlap + dllm + ray（4 subagent → 6 页）

**触发**：用户 `Turn 2`，承接同日 P4 batch entry 末尾「下一步建议」中的 Turn 2 路线图（小模块 rollup）。本轮选 6 个 ≤ 7 .py 的小模块，**4 个 subagent** 完成（其中 multiplex+tokenizer 配对、batch_invariant_ops+batch_overlap 配对，dllm/ray 各 solo）。

| 模块 | 文件量 | 选 Turn 2 理由 | subagent 配对 |
|---|---|---|---|
| `multiplex` | 2 .py / 15 KB | PD-Multiplexing；与 disaggregation 互斥需消歧 | A（与 tokenizer 配对） |
| `tokenizer` | 1 .py / 7 KB | 微模块；与 4 个同名 "tokenizer" 对象消歧高价值 | A |
| `batch_invariant_ops` | 2 .py / 33 KB | 确定性算子；与 sampling.md `--enable-deterministic-inference` 闭环 | B（与 batch_overlap 配对） |
| `batch_overlap` | 4 .py / 61 KB | TBO/SBO MoE overlap；与 TpModelWorker.md `delay_sample_func` 闭环 | B |
| `dllm` | 7 .py / 30 KB | Diffusion LLM；TpModelWorker 4 分支之一；**SGLang-unique** | C（solo） |
| `ray` | 5 .py / 29 KB | Ray actor 编排；与 Engine.md `_launch_scheduler_processes` 钩子闭环 | D（solo） |

**执行方式**：4 个 explore subagent **完全并行**（read-only 模式返回 source-anchored 草稿）+ 主 agent 写入 + normalize anchor 格式 + 协同更新。**所有 4 subagent 输出长度均 27-34 KB**，配对 subagent A/B 各产出 2 页（合计 4 页），solo C/D 各 1 页 → **总 6 页**。

**新建页（6 个）**

| 文件 | 大小 | 关键涵盖 |
|---|---|---|
| [sglang/modules/multiplex.md](../sglang/modules/multiplex.md) | ~12 KB | 2 .py + `PDMuxConfig` YAML + `initialize_stream_groups`（含 sgl_kernel.spatial green context）+ `SchedulerMultiplexMixin.event_loop_pdmux` + 全局 `STREAM_GROUPS`/`SM_COUNTS` + **与 disaggregation/PP/chunked-prefill/overlap 互斥**断言 + 4 CLI（`--enable-pdmux`/`--pdmux-config-path`/`--sm-group-num`） |
| [sglang/modules/tokenizer.md](../sglang/modules/tokenizer.md) | ~10 KB | **1 .py**：`TiktokenTokenizer` xtok JSON 加载 + 类 HF API（encode/decode/apply_chat_template/init_xgrammar）+ **`get_tokenizer` 在 `tokenizer_name.endswith(".json")` 时分发**；**4 个同名"tokenizer"对象消歧表**（本目录 / `srt/managers/tokenizer_manager.py` / `srt/utils/hf_transformers/tokenizer.py` / `srt/managers/multimodal_processor.py`）顶级 CONTRADICTION 块 |
| [sglang/modules/batch_invariant_ops.md](../sglang/modules/batch_invariant_ops.md) | ~13 KB | 2 .py + Triton 5 kernel（`matmul_kernel_persistent` / `_log_softmax_kernel` / `mean_kernel` / `bmm_kernel_persistent` / `_rms_norm_kernel`）+ `torch.library.Library("aten", "IMPL")` 注册 ATen CUDA + `enable_batch_invariant_mode()` 在 `--enable-deterministic-inference` 时由 `ModelRunner` 调用 + DeepGEMM/Triton 双路 + `--enable-deterministic-inference` 自动 `disable_piecewise_cuda_graph=True` + 改编自 thinking-machines-lab + RMSNorm 包装注释指向 vLLM |
| [sglang/modules/batch_overlap.md](../sglang/modules/batch_overlap.md) | ~16 KB | 4 .py + 4 类代码（operations stage 调度 / operations_strategy 按层 op 序列 / two_batch_overlap TBO / single_batch_overlap SBO）+ `execute_overlapped_operations` 双 executor `delta_stage` 交错 + `TboForwardBatchPreparer` batch 切分 + `MaybeTboDeepEPDispatcher` MoE A2A 双 dispatcher + `SboFlags` + `CombineOverlapArgs` CUDA stream/event + **`--disable-overlap-schedule` vs `--enable-two-batch-overlap` 是不同层概念** + `delay_sample_func` cross-link |
| [sglang/modules/dllm.md](../sglang/modules/dllm.md) | ~16 KB | 7 .py（无包根 `__init__.py`，仅 `algorithm/__init__.py`）+ `DllmConfig` HF 架构表（**3 个**：`LLaDA2MoeModelLM`/`SDARForCausalLM`/`SDARMoeForCausalLM`）+ `SchedulerDllmMixin` + `DllmManager` waiting/staging 队列 + `ForwardMode.DLLM_EXTEND` + **2 解码算法**（`LowConfidence` 默认 0.95 阈值 / `JointThreshold` M2T+T2T 编辑）+ FlashInfer DLLM 元数据 + `--dllm-algorithm` CLI + 推理时强制 `pp_size=1` / `disaggregation=null` / 无 LoRA 等 + **vLLM/MindIE grep 0 命中**：SGLang-unique |
| [sglang/modules/ray.md](../sglang/modules/ray.md) | ~16 KB | 5 .py + `RayEngine(Engine)` 子类化 `_launch_scheduler_processes` + `SchedulerActor` Ray actor（每 GPU 一个）+ `RayDataParallelController(DataParallelController)` 子类化 + `RaySchedulerInitResult` + Placement Group 自创建（STRICT_PACK / SPREAD）+ ZMQ 仍保留作为请求 IPC + `--use-ray` CLI（仅 1 字段，**无** ray_address / ray_num_*）+ **vs vLLM `RayDistributedExecutor` 更轻**：仅替换 scheduler 进程，不是第二套执行器抽象 |

**关键 synthesis（6 模块综合）**

1. **「同名 4 类 tokenizer」消歧**（[tokenizer.md](../sglang/modules/tokenizer.md) 顶级 CONTRADICTION 块）：`srt/tokenizer/`（本批新页，仅 TiktokenTokenizer）/ `srt/managers/tokenizer_manager.py`（前端进程 TokenizerManager）/ `srt/utils/hf_transformers/tokenizer.py`（`get_tokenizer` 工厂）/ `srt/managers/multimodal_processor.py`（多模态分发）—— **4 个对象都叫 / 含 "tokenizer" 但职责完全不同**，类似 P2 批 4 个 connector 命名陷阱。
2. **PD-Multiplexing vs PD-Disaggregation 互斥**（[multiplex.md](../sglang/modules/multiplex.md)）：两者都含 "PD" 但语义截然不同 — multiplex 是 **同 GPU 上 P/D 流复用**（SM 分区 + green context），disaggregation 是 **跨节点 P/D 进程分离**。`server_args` 显式断言两者 + chunked prefill + overlap 都不可同时开启（[L6510-6523](d:\design\sglang\python\sglang\srt\server_args.py)）。
3. **「overlap」3 层不同概念**（[batch_overlap.md](../sglang/modules/batch_overlap.md) 顶部 CONTRADICTION 块）：`--disable-overlap-schedule` 控制 **Scheduler ? GPU worker 流式重叠**（[`TpModelWorker.enable_overlap`](d:\design\sglang\python\sglang\srt\managers\tp_worker.py)）/ `--enable-two-batch-overlap` 控制 **MoE 双 micro-batch 前向交错**（本模块 TBO）/ `delay_sample_func` 控制 **grammar 约束下 sample 延后到下一步**（与本模块无 import 关系）。**"overlap" 在 SGLang 内是产品术语而非单一技术**。
4. **DLLM 是 SGLang-unique 设计点**（[dllm.md](../sglang/modules/dllm.md) Cross-project synthesis）：vLLM 与 MindIE 的 `dllm` / `LLaDA` / `Dllm` grep 全 0 命中。这是继 **outlines jump-forward**（constrained.md）+ **EPD encode_server**（disaggregation.md / multimodal.md）后的第 3 个 SGLang-unique 设计点；可作为新 cross compare topic 种子。
5. **Ray 模式 vs vLLM 设计哲学差异**（[ray.md](../sglang/modules/ray.md) Cross-project synthesis）：SGLang `srt/ray/` 仅 5 .py + 仅替换 scheduler 进程为 actor，**保留 ZMQ 协议**；vLLM `vllm/v1/executor/ray_executor.py::RayDistributedExecutor` 是 **完整 Executor 抽象的 4 实现之一**，覆盖 worker 创建 / forward / DAG。**"用 Ray" 在 SGLang 是局部启动器，在 vLLM 是一等执行器抽象**。
6. **batch_invariant_ops 谱系标注精确**：文件头明示 **改编自 thinking-machines-lab/batch_invariant_ops**（[L1-15](d:\design\sglang\python\sglang\srt\batch_invariant_ops\batch_invariant_ops.py)）+ RMSNorm 包装注释链 vLLM `batch_invariant`。这是继 [`compilation/`](../sglang/modules/compilation.md)（vLLM v0.10.0 fork）+ [`model_loader/`](../sglang/modules/model_loader.md)（vLLM v0.6.3-0.6.4 fork）+ [`mm_utils.py`](../sglang/modules/multimodal.md)（LLaVA-NeXT）+ [`conversation.py`](../sglang/modules/parser.md)（FastChat）后的又一血缘标注案例。
7. **TBO / SBO 双层 MoE overlap**（[batch_overlap.md](../sglang/modules/batch_overlap.md)）：TBO 通过 **batch 切分 + 双 executor `delta_stage` 交错**（[operations.py:30-58](d:\design\sglang\python\sglang\srt\batch_overlap\operations.py)）；SBO 通过 **MoE combine 与 down-gemm 之间 CUDA stream/event 重叠**（[single_batch_overlap.py:28-144](d:\design\sglang\python\sglang\srt\batch_overlap\single_batch_overlap.py)）。**两者正交，可同时启用**；与 [`eplb`](../sglang/modules/eplb.md) / [`elastic_ep`](../sglang/modules/elastic_ep.md) MoE 子系统强耦合。
8. **数字精度核对**：6 模块的文件计数与类层次均经 Glob + grep 直接匹配；其中 dllm 实测 7 .py（无包根 `__init__.py`，仅 `algorithm/__init__.py`），`DllmConfig.DLLM_PARAMS` 仅 3 架构（与文档示例的 LLaDA / Dream / Mercury 名称不一致 — 已加 CONTRADICTION）。

**协同更新（5 文件）**

| 文件 | 修改 |
|---|---|
| [sglang/index.md](../sglang/index.md) | 6 行 TODO → DONE：multiplex / tokenizer / batch_invariant_ops / batch_overlap / dllm / ray（含 P5 标 + 一句话摘要 + 同名陷阱标记 tokenizer） |
| [sglang/modules/disaggregation.md](../sglang/modules/disaggregation.md) | frontmatter `related:` 加 multiplex.md（PD 互斥消歧） |
| [sglang/entities/Engine.md](../sglang/entities/Engine.md) | frontmatter `related:` 加 ray.md（`_launch_scheduler_processes` 子类化 hook） |
| [sglang/entities/TpModelWorker.md](../sglang/entities/TpModelWorker.md) | frontmatter `related:` 加 dllm.md + batch_overlap.md（forward_batch_generation 4 分支 + delay_sample_func） |
| [sglang/entities/DataParallelController.md](../sglang/entities/DataParallelController.md) | frontmatter `related:` 加 ray.md（`RayDataParallelController` 子类化） |

**Markers added**

| 类型 | 数量 | 详情 |
|---|---|---|
| `[!todo] VERIFY` | **5** | multiplex 1（`docs/advanced_features/pd_multiplexing.md` 当前工作区未检出） + batch_invariant_ops 1（`log_softmax` docstring 含合并残留 `>> Stashed changes`） + batch_overlap 1（`OperationsStrategy.init_new_tbo` 仅支持 3 种 DecoderLayer 是否有遗漏）+ dllm 2（`--enable-dllm` 字面命名缺失 / Mercury/Dream 名称未在 DLLM_PARAMS） |
| `[!warning] CONTRADICTION` | **5** | multiplex 1（与 disaggregation 同含 "PD" 但语义不同）+ tokenizer 1（**4 个同名 "tokenizer"** 对象消歧表）+ batch_overlap 1（`--disable-overlap-schedule` vs `--enable-two-batch-overlap` 不同层概念）+ batch_invariant_ops 1（不声称与 vLLM 当前 tree 逐行等价）+ dllm 1（社区常用名 Mercury/Dream 与源码 DLLM_PARAMS 不一致）+ ray 1（不存在 `RayServingEngine` / `RayClusterClient` 等社区常见命名） |
| §跨子系统 N/A 强论断 | **18+** | 6 模块 × 各 3 处 |
| **sgl-kernel 算子绑定累计** | **0 新增 + 1 间接发现** | multiplex 通过 `sgl_kernel.spatial` 调用 green context API（非新算子注册）；维持 P4 后的 **7 个累计** |

**Anchor 抽查命中率**：6/6（multiplex `PDMuxConfig` L14-21 / tokenizer `TiktokenTokenizer` L29-167 + `get_tokenizer .json 分支 L448-451` / batch_invariant_ops `enable_batch_invariant_mode` 在 model_runner.py L676-680 / batch_overlap `execute_overlapped_operations` L30-58 + `TboForwardBatchPreparer` L472-803 / dllm `DLLM_PARAMS` 3 架构 L34-46 / ray `RayEngine._launch_scheduler_processes` 子类覆写 + `SchedulerActor` 1 actor 类）

**净变化**

| | before | after |
|---|---|---|
| sglang/modules DONE 数 | 22/30 | **28/30**（+6：multiplex/tokenizer/batch_invariant_ops/batch_overlap/dllm/ray） |
| sglang 总页数 | 29 | **35** |
| 三家 entities 总数 | 24 | 24（无变化） |
| comparison topics DONE 数 | 14/24 | 14/24 |
| 总 .md 文件数（excluding raw/ + log-archive/） | 93 | **99**（+6） |
| sglang **非 models/layers** 模块剩余 TODO | 14 | **8**（剩余：entrypoints/anthropic + entrypoints/ollama + grpc + observability + configs + debug_utils + 2 大模块 models/layers） |
| **SGLang-unique 设计点累计** | 2（outlines jump-forward / EPD encode_server） | **3**（+ DLLM diffusion text 路径） |

**Lessons learned**

1. **小模块配对（pair）模式有效**：本批 4 subagent → 6 页（multiplex+tokenizer 配对 / batch_invariant_ops+batch_overlap 配对 / dllm + ray solo），相比 P3/P4 的「1 subagent 1 模块」节省 33% subagent 槽位。**配对前提**：两模块都 ≤ 7 .py 且语义相关（PD 复用 vs 同 PD 子系统 ≠ 配对；invariant ops vs overlap 都是「batch 路径变种」可配对）。
2. **subagent 输出大小再次验证**：本批 4 subagent 输出 27-34 KB（远超 P3 批 constrained 23 KB 截断阈值）；配对模式（A/B subagent）的输出虽含 2 页内容但平均每页 ~13 KB，仍很健康。
3. **同名陷阱模板沉淀**：本批 [tokenizer.md](../sglang/modules/tokenizer.md) **4 个同名对象消歧表** 是继 P2 批 4 个 connector / P3 批 4 模块名（function_call/constrained/parser）后的第 3 次同名陷阱；建议 [AGENTS.md §7](../AGENTS.md) lint 项加入「**模块命名 grep 检查**」：新 ingest 模块时必须搜全仓库同名/近似命名，防遗漏。
4. **"overlap 3 层概念"是 cross-page synthesis 的高价值发现**：单看 `batch_overlap/`、`tp_worker.py`、`server_args.py` 任一个都不会看到 3 层完整图景；**只有跨 3 个 wiki 页交叉对照才能解构这个产品术语**。这种"术语解构 synthesis"是 wiki 累积价值最大的产物之一。
5. **SGLang-unique 设计点累计已达 3 个**：outlines jump-forward + EPD encode_server + DLLM diffusion。这种「N 家中独 1 家有」的差异点是 cross compare 的核心产出；下次 cross compare topic 应优先考虑 **`compare unique-design-points across all`**（汇总三家各自独有特性矩阵）。
6. **小模块 entry 大小符合 ~3KB/模块经验**：本批 6 模块 entry ~18 KB，与 archive entry 末尾 lessons learned 中 "entry 大小 ≈ 模块文件量 × 0.05-0.1" 一致。

**当前 wiki 状态**

| 项目 | overview | index | modules | entities | topics | 总数 |
|---|---|---|---|---|---|---|
| MindIE-LLM | ? | ? | 3/14 | 9/9 | 8/13 | 20 |
| vLLM | ? | ? | 2/26 | 10/17 | 5/14 | 17 |
| SGLang | ? | ? | **28/30**（+6） | 5/11 | 2/14 | **35** |
| comparison | — | ? | — | — | 14/24 | 14 + dimensions |
| 顶层 meta | 5 | — | — | — | — | 5 |

**总 .md 文件数**：**99**（excluding `wiki/raw/` + `wiki/log-archive/`）；**105**（including 6 archive files）。delta +6。

**§11 log rotation 阈值复核**（append 本 entry + 上面的 archive entry 后）：bytes ~85-90 KB / lines ~700 / entries 7 → 仍在 110 KB / 1500 行阈值内。

**下一步建议（"all" 完成进度：28/30 模块）**

| Turn | 命令 | 模块（剩余） |
|---|---|---|
| **Turn 3**（合并 entrypoints rest + utility 大批）| `Turn 3` 或 `ingest sglang modules rest` | `entrypoints/anthropic` (3.py) + `entrypoints/ollama` (4.py) + `grpc` (1.py 占位) + `observability` (10.py) + `configs` (44.py 多 dataclass) + `debug_utils` (89.py 多小文件)。可 4-5 subagent 并行 |
| **Turn 4**（独立大）| `Turn 4` 或 `ingest sglang modules models` | **186 .py / 3.9 MB** — 按模型族分组（参考 multimodal processors matrix 模式） |
| **Turn 5**（独立大）| `Turn 5` 或 `ingest sglang modules layers` | **252 .py / 4.1 MB** — 按 attention / MoE / linear / quant / norm / rotary 等子目录分组 |

**完成 "all" 后**（28+5+1+1=35? 实际剩 8 个 TODO + 2 大）后建议优先：

1. `compare unique-design-points across all` — 汇总三家独有特性矩阵（SGLang 的 outlines jump-forward + EPD encode_server + DLLM 已成清单）
2. `compare model-loading across all` — 4 条权重通道在三家的对偶矩阵
3. `compare openai-compat across all` — vLLM `vllm/entrypoints/openai/` 是 SGLang 直接谱系
4. `verify sglang/modules/*` — 本批 6 页 + P4 批 5 页 + P3 批 6 页累积 28+ markers 待消化

---
