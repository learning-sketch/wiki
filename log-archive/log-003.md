---
type: log-archive
project: cross
status: verified
confidence: high
verified_against: 2026-04-18
sources: []
related:
  - ../log.md
  - ../AGENTS.md
  - ./log-001.md
  - ./log-002.md
---

# Wiki Activity Log — Archive 003

> **Archive period**: 2026-04-18 中段（4 条 entries：archive log rotation 001/002 + pin source-versions + lint post pin/rotation health check + verify comparison/topics/speculative-decoding）
> **Archived on**: 2026-04-18（与本批次所属同日，因 ingest 节奏快导致 log.md 再次超 110 KB 触发轮转）
> **Original line range in `log.md`**: 822-1056（轮转前）
>
> 本归档文件 **append-only / 不可重写**。新 entries 一律 append 到 [`../log.md`](../log.md)。

---

## [2026-04-18] archive | wiki | log rotation 001/002（按方案 A 触发）

**触发**：用户反馈"log.md 过大"。轮转前 `log.md` = 1971 行 / ~143 KB / 44 entries —— 超 [AGENTS.md §11](../AGENTS.md) 阈值 (1500 行 / 110 KB)。

**轮转结果**

| 文件 | 期间 | entries | 大小 | 备注 |
|---|---|---|---|---|
| **新建** [log-archive/log-001.md](./log-001.md) | 2026-04-17 | 21 | ~70 KB | 04-17 全部 ingest/lint/verify entries |
| **新建** [log-archive/log-002.md](./log-002.md) | 2026-04-18 早段 | 17 | ~110 KB | compare 三批 + lint-fix p0-p4 + 2 schema-update + entity P0/P1 + topic P0 + verify moe + 2 ingest topic + 1 compare spec |
| **重写** [log.md](../log.md) | 当前 | 6 + 本条 | ~85 KB | header + Archive Index + 6 最近 entries + 本 archive 条目 |

**轮转策略**（落地版）

1. **触发阈值**：log.md > 1500 行 OR > 110 KB（任一满足）
2. **归档目标**：`wiki/log-archive/log-NNN.md`（001 起，append-only，从不重写）
3. **保留策略**：主 log.md 保留 **header + Archive Index + 最近 ~10 条 entries**
4. **schema-update entries**：本次轮转放进 log-002.md（AGENTS.md 本身已是 schema 真相，log 中 entries 仅作历史轨迹）
5. **链接修复**：归档文件内的 wiki 路径相对从 `log-archive/` 出发（即 `../mindie/...` 而非 `mindie/...`）；`d:\design\<...>` 绝对路径不变

**协同更新**

- [AGENTS.md](../AGENTS.md) 新增 §11 Log rotation（触发阈值 / 归档目录约定 / 保留策略 / `lint` 时检查）
- [AGENTS.md §5 step 7](../AGENTS.md) "更新 index 和 log" 末尾追加："`log.md` append 后若超阈值，触发 §11 轮转"

**净变化**

| | before | after |
|---|---|---|
| log.md 行数 | 1971 | ~600（节省 ~70%）|
| log.md 字节 | ~143 KB | ~85 KB |
| log.md entries | 44 | 6 + 本条 = **7** |
| 归档文件数 | 0 | **2** |
| 归档总字节 | 0 | ~180 KB（与 log.md 历史完全一致，无信息丢失）|
| 单文件最大 entries 阈值 | 无 | **30** |

**下一次轮转预期**

按当前 ingest/verify/compare 节奏（每天 ~10 entries），**约 3-5 天后**会再次触发。届时主 log.md 会归档为 log-003.md（接续编号），保持 7-10 条最近 entries + Archive Index 表的轻量结构。

---

## [2026-04-18] pin | source-versions | initial pin + 增量工作流 schema 沉淀

**触发**：用户 "记录三仓 commit/branch + 后续基于新增代码做增量"。

**新建** [source-versions.md](../source-versions.md)（~3.5 KB）：

| 项目 | branch | commit (short) | last commit date | last commit msg |
|---|---|---|---|---|
| MindIE-LLM | `master` | `f032cd3f` | 2026-04-15 22:48:40 +0800 | `!901 merge dev into master` |
| vLLM | `main` | `5f7fab88` | 2026-04-16 09:55:29 +0800 | `[ROCm][FEAT] Integrate aiter gemm w8a8 ptpc (#33773)` |
| SGLang | `main` | `34fef07a` | 2026-04-15 20:03:44 -0700 | `Upgrade transformers to 5.5.3 and refactor hf_transformers_utils into subpackage (#21569)` |

**MindIE-LLM working tree caveat**：2 个 untracked 文件被 wiki 引用 —— `docs/mindie_generator_aclgraph_pp_design.md`（→ [mindie/topics/aclgraph-pp.md](../mindie/topics/aclgraph-pp.md) 唯一来源）+ `mindie_llm/runtime/utils/distributed/pipeline_parallel.py`（→ [mindie/entities/ParallelInfoManager.md](../mindie/entities/ParallelInfoManager.md) 草稿引用）。增量分析时这两个文件需单独 diff（不在 master 历史中）。

**Schema 沉淀**

- [AGENTS.md](../AGENTS.md) **新增 §12 Incremental update workflow**：5 步增量流程（diff 摸底 → 影响面 grep → 逐页处理按改动量分层 → frontmatter 更新 → source-versions + log 同步），含"重大变更启发式"5 个信号（refactor 大批 / 新大目录 / BREAKING / merge dev into master 等）+ 与其它 6 个工作流的关系表
- [AGENTS.md §10](../AGENTS.md) 末尾加引用：`所有 verified_against 字段背后的精确 commit / branch 见 source-versions.md`
- [index.md](../index.md)：`related:` 加 `source-versions.md`；§操作入口 加 "上游代码增量" + "三仓代码版本 pin" 入口

**核心设计决策**

1. **`verified_against: YYYY-MM-DD` 字段语义不变** —— 仍用日期；但日期背后的精确 commit 由 [source-versions.md §History](../source-versions.md) 表查得
2. **§Current pins 段是唯一 mutable 区域**；§History 严格 append-only，每次增量后追加一行
3. **增量优先用 `verify`，不要 fresh re-ingest** —— wiki 是派生物，commit diff 通常只动几个文件 / 几个锚点行号；fresh re-ingest 浪费时间且会丢失已积累的 lessons learned
4. **MindIE 的 untracked 文件特殊处理** —— 因为是用户本地补丁不入 git 历史，增量时必须单独 `git diff /dev/null <file>` 看变化（已在 source-versions.md working tree state 段标注）

**预期使用场景**

- 用户拉新 vllm / sglang main → `cd <repo>; git log <pin>..HEAD --oneline` → 短 list 直接 `verify` 受影响页；长 list 启 multi-subagent 并行
- MindIE 上游 dev branch 合入 master → `git log master..dev --stat` → 按 §12 启发式判断 ingest vs verify
- 半年后回看 wiki 是否仍有效 → 看 source-versions.md `Current pins` 段 + 跟当前三仓 HEAD 对比，没动就直接信任 wiki，动了走 §12

**净变化**

| | before | after |
|---|---|---|
| 总 .md 文件数 | 56 | **57** |
| 全局 schema 章节数 (AGENTS.md) | 11 | **12**（§12 新增 ~80 行） |
| 上游代码版本可追溯性 | wiki 仅有 `verified_against: 日期`，日期对应 commit 不可考 | **commit-level pin** + History 追溯 |
| 增量分析工作流 | 隐式（无 schema） | **5 步显式流程 + 启发式表** |

**下一步建议**

- 用户主动操作时机：**上游拉新代码后 → `increment`** 触发 §12
- 或现在主动跑一次试运行：`cd d:\design\vllm; git fetch; git log 5f7fab88..origin/main --oneline` 看远端是否已有新 commit；有的话验证一次完整 §12 流程

---

## [2026-04-18] lint | wiki | post pin/rotation health check（含 §11/§12 首次集成验证）

**触发**：用户 `lint`。承接刚完成的 §11 log rotation + §12 source-versions.md pin 两个 schema 沉淀。

**总体统计**

| 项 | 值 |
|---|---|
| Markdown 文件总数 | **63**（content 60 + log 3 含 archive + 3 schema/index 等） |
| 总字节 | ~1498 KB |
| `status: verified` | 60（除 3 overview 设计上保持 draft）|
| `status: draft` | 3（mindie/vllm/sglang overview，与 [AGENTS.md §3](../AGENTS.md) 设计一致：概览页持续在变） |
| 未带 frontmatter 的文件 | **1**（[README.md](../README.md) — 用户面向的 readme，非 agent schema 页面，设计如此） |

**按目录分布**

| 目录 | .md 数 |
|---|---|
| `wiki/` 顶层 | 5（README/AGENTS/log/index/source-versions） |
| `wiki/log-archive/` | 2（log-001 + log-002） |
| `wiki/comparison/` | 2 + topics/ 10 = **12** |
| `wiki/mindie/` | 2 + entities/ 9 + modules/ 3 + topics/ 8 = **22** |
| `wiki/vllm/` | 2 + entities/ 4 + modules/ 2 + topics/ 5 = **13** |
| `wiki/sglang/` | 2 + entities/ 2 + modules/ 3 + topics/ 2 = **9** |

**Lint 检查表（10 项）**

| # | 检查 | 结果 | 详情 |
|---|---|---|---|
| 1 | **frontmatter 完整性** | **PASS** | 60/60 内容页有 `type / project / status / confidence / verified_against / sources / related` 7 字段；`README.md` 设计上无 frontmatter（不是 agent schema 页） |
| 2 | **status / verified_against 抽查** | **PASS** | 全部 status 合规（verified / draft 二选一）；verified_against 全部 `2026-04-17` 或 `2026-04-18`；唯一变体格式 `mindie/topics/moe.md`（含 verify pass 注释）符合 [§3 模板](../AGENTS.md) 允许的"日期 + 备注"格式 |
| 3 | **死链 (待建 placeholder)** | **PASS（合理占位）** | 4 处 `(待建)` 全部指向 [comparison/topics/engine-architecture.md](../comparison/topics/engine-architecture.md) 与 [executor-worker.md](../comparison/topics/executor-worker.md) ——两页在 [comparison/index.md:51-52](../comparison/index.md) 显式列为 TODO，符合 forward-looking placeholder 约定 |
| 4 | **抽查源码 anchor 命中率** | **PASS（6/6）** | 1) `extract_hidden_states.py:87-89` "doesn't actually perform speculation" ✅；2) `factory.py:149` `register_connector("ExampleConnector", ...)` ✅；3) `block_pool.py:34` `class BlockHashToBlockMap:` ✅；4) `setup.py:132` `mindie_llm_backend = mindie_llm.connector.main:main` ✅；5) `eagle_worker_v2.py:138-155` `self.draft_worker = TpModelWorker(... is_draft_worker=True ...)` ✅；6) `prefix_cache_block_allocator.h:116` `std::unordered_map<HashValue, BlockId> cachedBlocks_;` ✅ |
| 5 | **§11 log rotation 阈值** | **PASS（无需轮转）** | log.md = 514 行 / 53 KB（轮转后增量），未达阈值 (1500 行 / 110 KB)；下次预计 3-5 天后再触发 → log-003.md |
| 6 | **§12 source-versions HEAD 对照** | **PASS（三仓 HEAD 全部匹配 pin）** | MindIE `f032cd3f...` ≡ pin；vLLM `5f7fab88...` ≡ pin；SGLang `34fef07a...` ≡ pin。**所有 wiki 页 `verified_against: 2026-04-17/18` 仍有效**，无需触发增量 |
| 7 | **Archive Index 准确性** | **PASS** | log.md Archive Index 段标注 log-001 = 21 entries / 70 KB、log-002 = 17 entries / 110 KB，与实际 `Get-Item` 结果（533 行 / 38 KB、1075 行 / 92 KB）一致；行数 vs entries 比有差距是因为含归档 frontmatter / Archive 头部段 |
| 8 | **mermaid 语法（抽查近期 ingest 页）** | **PASS** | vllm/topics/{prefix-cache, kv-connector, spec-decode-eagle}.md + mindie/modules/connector.md + mindie/topics/{prefix-cache, structured-output}.md 共 6 页含 mermaid，节点 ID 全 PascalCase / 下划线，特殊字符标签全 quote |
| 9 | **跨页交叉链接（spot check）** | **PASS** | source-versions.md ↔ index.md / AGENTS.md / log.md 三方互链；vllm/topics/spec-decode-eagle.md ↔ comparison/topics/speculative-decoding.md 双向 related；mindie/topics/prefix-cache.md ↔ vllm/topics/prefix-cache.md ↔ comparison/dimensions.md §dim-prefix-cache 三角互链 |
| 10 | **新 schema 段 (§11/§12) 互链** | **PASS** | AGENTS.md §10 末尾加了 source-versions.md 引用；§5 step 7 加了 §11 轮转检查触发；§12 末尾的"与其它工作流的关系表"列出 6 个工作流的协同 |

**Marker 分布（content 页面）**

| 文件 | active markers |
|---|---|
| 最高密度（≥ 8） | vllm/topics/spec-decode-eagle.md (11)、vllm/topics/kv-connector.md (11)、vllm/topics/prefix-cache.md (9)、mindie/topics/prefix-cache.md (9)、comparison/topics/async-schedule.md (8)、comparison/topics/distributed.md (8) |
| 关键 RESOLVED/RECONFIRMED | mindie/topics/moe.md（2 RESOLVED + 1 RECONFIRMED + 1 active CONTRADICTION）|
| 总累计 | ~155+ active markers across 50 content pages（平均 ~3 个/页，与典型 verify-driven knowledge base 持平） |

> **Top-10 marker 密度页**多为本周深度 ingest 的新页（vLLM P1 + MindIE P1）；属"高深度 = 多 caveat"自然结果，不是 wiki 健康问题。建议下一轮 `verify` 优先选 vllm spec-decode-eagle 与 kv-connector（11 个 markers）。

**§5 step 3 hidden cross-reference grep 累积**

| 累计 | 数 |
|---|---|
| 总 N/A 强论断条数（"在 X 全树 grep N 命中" 模式）| **~30+**（mindie 端 ~15 + vllm 端 ~15+）|
| 含本规则的 entity / topic / module 页 | **9 + 3 = 12**（mindie P0/P1 entity + topic + module + vllm P1 topic 全部）|

**新发现的小问题（本轮 lint 不修，留作下次）**

无。本轮 lint 健康度评级 **A**（无 P0/P1 问题；P2/P3 仅有"高 marker 密度页可考虑下次 verify 消化"的常规提醒）。

**当前 wiki 状态（与 source-versions.md 同步）**

| 项目 | overview | index | modules | entities | topics | demo 总数 |
|---|---|---|---|---|---|---|
| MindIE-LLM | ✅ | ✅ | 3/14 | 9/9 | 8/13 | 20 |
| vLLM | ✅ | ✅ | 2/26 | 4/17 | 5/14 | 11 |
| SGLang | ✅ | ✅ | 3/30 | 2/11 | 2/14 | 7 |
| comparison | — | ✅ | — | — | 10/24 | 10 + dimensions |

**总计 63 .md（57 内容/index/log/schema 页 + 2 归档 + 1 README + 1 source-versions + 1 AGENTS + 1 log）**

**下一步建议**

按 ROI 排序：

1. **`verify comparison/topics/speculative-decoding.md`** —— 5 个 vLLM 相关 VERIFY 现在可用 [vllm/topics/spec-decode-eagle.md](../vllm/topics/spec-decode-eagle.md) 的 anchor 直接 RESOLVED；ROI 最高（已就绪、低风险、消化已有 marker）
2. **`compare prefix-cache across all`** —— mindie + vllm 双 prefix-cache 页 + sglang mem_cache.md 已就绪，"hash table vs trie 三家两派"的 synthesis 种子已在两个 prefix-cache 页中明示；走 `compare` 工作流可继承多页 anchor，工作量 < 1 turn
3. **`ingest vllm topics paged-attention`** —— P1 收尾，与 KV connector + prefix cache 形成 v1 KV cache 完整图谱
4. **被动等待** —— 三仓 HEAD 都未动，可暂不触发 §12；用户拉新代码后再说

---

## [2026-04-18] verify | comparison/topics/speculative-decoding | 5 markers triaged (5 RESOLVED + 0 active)

**触发**：用户 `verify comparison/topics/speculative-decoding.md`，承接刚完成的 [vllm/topics/spec-decode-eagle.md](../vllm/topics/spec-decode-eagle.md) ingest（提供大量 vLLM 端 anchor）+ MindIE / SGLang 端复读源码补 anchor。

**Markers triaged（全部 RESOLVED）**

| # | 类型 | 原始 marker | 解决方式 |
|---|---|---|---|
| 1 | CONTRADICTION | vLLM `EagleSpeculator` vs `SpecDecodeBaseProposer` 双 EAGLE 实现并存 | RESOLVED via [vllm/topics/spec-decode-eagle.md §4 EAGLE 双路径](../vllm/topics/spec-decode-eagle.md)：8 维度对照表已澄清两路径分工——老 `EagleProposer(SpecDecodeBaseProposer)` 由老 `gpu_model_runner.py:561` 实例化（支持 tree spec / parallel drafting / 7 method 通用）；新 `EagleSpeculator` 由新 `gpu/model_runner.py:172` 通过 `init_speculator()` 工厂创建（独立 `InputBuffers` + 双 `EagleCudaGraphManager`，仅链式 spec + 仅 4 method）。**选择由 ModelRunner 类型决定** |
| 2 | CONTRADICTION | SGLang `disable_overlap_schedule` 多处自动开关 | RESOLVED via 源码复读 [server_args.py:3251-3295, 3380](d:\design\sglang\python\sglang\srt\server_args.py)：实为 **3 段顺序逻辑的有意 cascade**——DFLASH/NGRAM 强制 disable；EAGLE/EAGLE3/STANDALONE + `SGLANG_ENABLE_SPEC_V2=False` auto disable；同算法 + `=True` 重新启用。`disable_overlap_schedule` 是 **算法 × env 联合决定的派生量**，不是矛盾 |
| 3 | VERIFY | vLLM `dflash` 在 `EagleModelTypes` 字面归类 + `SpeculativeMethod` 与类对应 | RESOLVED via [vllm/topics/spec-decode-eagle.md §2 SpeculativeMethod 完整枚举 + synthesis 表](../vllm/topics/spec-decode-eagle.md)：`EagleModelTypes = Literal["eagle", "eagle3", "extract_hidden_states", MTPModelTypes, DFlashModelTypes]` 字面包含 dflash；`use_eagle()` 返回 `True` 把 dflash 当 EAGLE 通路；但实例化 proposer 是独立 `DFlashProposer(SpecDecodeBaseProposer)`（强制 parallel_drafting + 跨注意力非因果）。完整 7 类 method ↔ class 映射在 §2 |
| 4 | VERIFY | SGLang draft+target KV pool 共享一致性 | RESOLVED via 源码复读 [eagle_worker_v2.py:123-152](d:\design\sglang\python\sglang\srt\speculative\eagle_worker_v2.py) + [tp_worker.py:340-361](d:\design\sglang\python\sglang\srt\managers\tp_worker.py)：一致性由 **单 allocator + 单 req_to_token_pool** 两个共享对象保证（`target_worker.get_memory_pool()` 返二元组 → 透传给 draft `TpModelWorker(is_draft_worker=True, ...)`）。draft 与 target ModelRunner 各自 load 自己的 KV cache 张量层，但**索引到同一份 slot id 空间**——单 allocator 防双重分配（无 race） |
| 5 | VERIFY | MindIE `MtpWorkerExp` vs `MtpWorker` 实质语义差 | RESOLVED via 复读 [spec_worker.py:23-707](d:\design\MindIE-LLM\mindie_llm\runtime\model_runner\spec_worker.py)：两类核心 dual-ModelRunner 模式完全相同（`main_model_runner` + `draft_model_runner` via `is_draft_model=True`）。**关键差异**：`MtpWorkerExp` = `MtpWorker` + aclgraph/compile 支持（`compile()` / `set_eager_mode_with_padding()`） + `npu_cache` / `forward_context` / `build_forward_context` 三参数透传 + `is_dp_and_server_centralized` DP 模式适配。功能上 Exp **是 superset**。switch by `ENV.model_runner_exp` flag |
| 6 | VERIFY | vLLM `Scheduler.use_eagle` 触发分支 | RESOLVED via grep [v1/core/sched/scheduler.py](d:\design\vllm\vllm\v1\core\sched\scheduler.py) 全文：5 处使用 + 2 处独立分支 —— L214 init / L218-219 设值 / L229 透传 KVCacheManager / L330-331 chunked prefill `last_cache_position` 减 1 block "eagle prune" / L434, L703 `shift_computed_tokens=1`。配套 L218-222 设 `num_lookahead_tokens` |

**新发现 markers**：无（cross-check 未触发新一轮"一家有、另两家漏"）。

**Anchor-driven cross-check 复查**

§Anchor-driven cross-check 章节（line 337-391）已写完 2 个 anchor（MindIE `PLACEHOLDER_TOKEN` / `MtpWorker` 双 ModelRunner），三方扫描结果完整。本轮 verify 没新增 anchor 反向扫；现有论断仍准确：
- vLLM `_spec_token_placeholders = [-1] * num_spec_tokens`（[async_scheduler.py:16](d:\design\vllm\vllm\v1\core\sched\async_scheduler.py)）= placeholder 真等价物
- SGLang `accept_length` + `EagleDraftInput` dataclass = 不通过占位 token id 而用 dataclass 持张量
- vLLM `EagleSpeculator` 独立 `InputBuffers` / `BlockTables` = "双状态对象"非"双 ModelRunner"
- SGLang `is_draft_worker=True` `TpModelWorker` = 与 MindIE `draft_model_runner` 命名几乎一致的真等价物

**净变化**

| | before | after |
|---|---|---|
| comparison/topics/speculative-decoding.md markers active | 2 CONTRADICTION + 5 VERIFY = **7** | 0 active + 7 RESOLVED |
| frontmatter `verified_against` | `2026-04-18` | `2026-04-18 (verify pass: 2026-04-18)` |
| 新加 source 锚点 | — | **+2**：[sglang/python/sglang/srt/managers/tp_worker.py:340-361](d:\design\sglang\python\sglang\srt\managers\tp_worker.py)（VERIFY 4 KV pool 透传链）+ vLLM `Scheduler.use_eagle` 5 处使用全清单（VERIFY 6） |
| 跨页跟进项 | 0 | **+1**：MindIE `is_draft_model=True` 是否 alias 主模型 KV pool（VERIFY 4 末尾备注），下一轮 `mindie/topics/speculative.md` ingest 跟进 |
| §dim-spec 行需调整 | — | 无（cells 已富 anchor，verify pass 仅 markers RESOLVED 不改结构） |

**Lessons learned**

1. **"先深度 ingest 一家专题再 verify 跨家对比页"= 极高 ROI 的 verify pattern**：本轮 5 个 markers 中 3 个（CONTRADICTION 1、VERIFY 1+5、VERIFY 6）直接借力 [vllm/topics/spec-decode-eagle.md](../vllm/topics/spec-decode-eagle.md)（同日早段刚 ingest 的 ~579 行 vLLM 专题页）的 anchor 和 synthesis 表完成 RESOLVED；不需要重复全文 grep。这印证 [vllm topics P1 batch acceptance lessons learned](#) 第 1 条 —— "compare 页驱动 entity/topic 深度 ingest，topic 反哺 compare 页 RESOLVE 是良性循环"。
2. **CONTRADICTION 不一定是"页面错描"，也可能是"上游有意 cascade"的事实记录**：CONTRADICTION 2（SGLang `disable_overlap_schedule` 多处开关）在源码层是 3 段顺序逻辑的有意设计，本页早期 ingest 时把它当矛盾标，verify pass 复读后澄清为"派生量"。建议：CONTRADICTION 标注前先确认是否上游真矛盾，避免误标。
3. **跨页 marker 跟进要在 RESOLVED 文末显式留备注**：VERIFY 4 部分解决（SGLang 端清楚，但 MindIE 端 KV pool alias 行为未 ingest），用"末尾备注 + 链回 mindie/topics/speculative.md + 注明下一轮跟进"的写法保留 trace；不要一刀切关闭。
4. **复读全文 grep 的成本远低于 fresh re-ingest**：VERIFY 6 (`Scheduler.use_eagle`) 仅 grep 1 文件即得 5 处使用 + 1 个分支细节（"eagle prune"），<2 分钟完成；这是 [§12 increment workflow](../AGENTS.md) 推荐的"先 grep 再 verify"模式的实证。
5. **frontmatter `verified_against` 加 verify pass 备注**采用与 [mindie/topics/moe.md](../mindie/topics/moe.md) 同模板（`2026-04-18 (verify pass: 2026-04-18)`）；当 ingest 日 = verify 日时备注虽显冗余，但保留格式一致以便未来真正分离的增量场景。

**当前 wiki 状态**

| 项目 | overview | index | modules | entities | topics | demo 总数 |
|---|---|---|---|---|---|---|
| MindIE-LLM | ✅ | ✅ | 3/14 | 9/9 | 8/13 | 20 |
| vLLM | ✅ | ✅ | 2/26 | 4/17 | 5/14 | 11 |
| SGLang | ✅ | ✅ | 3/30 | 2/11 | 2/14 | 7 |
| comparison | — | ✅ | — | — | 10/24（speculative-decoding **markers 0/7 active**） | 10 + dimensions |

**总 .md 文件数**：63（无新增 / 无删除；本 verify pass 只更新 1 内容页 + 1 log entry）。

**下一步建议**

1. **`verify comparison/topics/async-schedule.md`**（active markers 8）—— 高密度 marker 页，类似本轮 ROI 应类似（部分 vllm 相关 VERIFY 可能也能借 [vllm/topics/spec-decode-eagle.md](../vllm/topics/spec-decode-eagle.md) §5/§6 的 anchor 解）
2. **`verify vllm/topics/kv-connector.md`**（active markers 11）—— 若用户后续 `compare kv-cache across all` 时这些 markers 应优先消化
3. **跟进 VERIFY 4 末尾备注**：下一轮 ingest [mindie/topics/speculative.md](../mindie/topics/speculative.md) 时 verify `is_draft_model=True` 是否 alias 主模型 KV pool；如不 alias → 与 SGLang 形成"draft 是否独立 KV pool"维度的对比
